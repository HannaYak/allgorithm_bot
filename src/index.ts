import { Telegraf, Markup, session, Scenes } from 'telegraf';
import express from 'express';
import { drizzle } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import { eq, or, inArray, and, desc, asc } from 'drizzle-orm';
import * as schema from '../drizzle/schema'; 
import 'dotenv/config';
import Stripe from 'stripe';
import { DateTime } from 'luxon';
import * as SD from './speedDating';
import { users, events, bookings, vouchers, secretLikes } from '../drizzle/schema';


// --- 1. НАСТРОЙКИ ---

async function broadcastToEvent(eventId: number, message: string) {
  const bookings = await db.query.bookings.findMany({
    where: and(eq(schema.bookings.eventId, eventId), eq(schema.bookings.paid, true))
  });

  // СОЗДАЕМ Set именно из Telegram ID
  const uniqueTgIds = new Set<number>();
  
  for (const b of bookings) {
    const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
    if (u?.telegramId) uniqueTgIds.add(u.telegramId);
  }

  // Рассылаем только по уникальным «адресам»
  for (const tgId of uniqueTgIds) {
    bot.telegram.sendMessage(tgId, message, { parse_mode: 'HTML' })
      .catch((err) => console.error(`Ошибка отправки на ${tgId}:`, err));
  }
}

if (!process.env.DATABASE_URL) throw new Error('DATABASE_URL is missing');
const client = postgres(process.env.DATABASE_URL);
const db = drizzle(client, { schema });

if (!process.env.STRIPE_SECRET_KEY) throw new Error('STRIPE_SECRET_KEY is missing');
const stripe = new Stripe(process.env.STRIPE_SECRET_KEY, { apiVersion: '2023-10-16' });

const GAME_PRICES: Record<string, string> = {
  'talk_toast': 'price_1T427MHhXyjuCWwf7CK0DvCA', 
  'talk_thematic': 'price_1T427MHhXyjuCWwf7CK0DvCA',
  'stock_know': 'price_1SUTkoHhXyjuCWwfxD89YIpP',
  'speed_dating_25_35': 'price_1SUTlVHhXyjuCWwfU1IzNMlf', // ID товара на 50 PLN
  'speed_dating_35_45': 'price_1SUTlVHhXyjuCWwfU1IzNMlf',
  'talk_toast_review': 'price_1SiDMGHhXyjuCWwfzysRSphU',
  'stock_know_review': 'price_1SiDKoHhXyjuCWwfwg24Y7mF',
};
const STRIPE_COUPON_ID = '8RiQPzVX'; 
const ADMIN_ID = 5456905649; 
const PROCESSED_AUTO_ACTIONS = new Set<string>(); 

const BEAUTY_NAMES: Record<string, string> = {
  'talk_toast': 'Talk & Toast 🥂',
  'stock_know': 'Stock & Know 🧠',
  'speed_dating_25_35': 'Speed Dating (25-35 лет) 💘',
  'speed_dating_35_45': 'Speed Dating (35-45 лет) 💘',
  'talk_thematic': 'Тематический ужин 🎭',
  'talk_toast_review': 'Talk & Toast 🎥 (со съёмкой)',
  'stock_know_review': 'Stock & Know 🎥 (со съёмкой)'
};

// Функция-помощник
const getGameName = (type: string) => BEAUTY_NAMES[type] || type;

const TYPE_MAP: Record<string, string> = { 
  'talk_toast': 'tt', 
  'stock_know': 'sk', 
  'speed_dating': 'sd',
  'talk_toast_review': 'ttr',
  'stock_know_review': 'skr'
};
const REV_TYPE_MAP: Record<string, string> = { 
  'tt': 'talk_toast', 
  'sk': 'stock_know', 
  'sd': 'speed_dating',
  'ttr': 'talk_toast_review',
  'skr': 'stock_know_review'
};

// --- 2. КОНТЕНТ (ПОЛНЫЙ) ---

const MINI_GAMES_TEXT = `🎮 <b>4 Мини-игры для разминки:</b>\n\n1. <b>«Две правды, одна ложь»</b>\n2. <b>«Я никогда не...»</b>\n3. <b>«Кто скорее всего?»</b>\n4. <b>«Контакт»</b>`;

const CONVERSATION_TOPICS = [
  "Если бы ты мог/ла пригласить кого-нибудь на ужин(из мёртвых или живых), кого бы ты выбрал/а и почему?", 
  "Хотел/а бы ты быть знаменитым/ой? Если да, то чем?", 
  "Прежде чем сделать звонок, ты репетируешь свою реплику?", 
  "Когда ты в последний раз пел/а в одиночестве?", 
  "Если бы ты мог/ла прожить до 100 лет, сохранив разум или тело 30-летнего, что бы выбрал/а?", 
  "За что ты испытываешь наибольшую благодарность?", 
  "Если бы ты мог, что бы ты изменил/а в воспитании себя?", 
  "Если бы ты мог/ла проснуться с новым умением, что бы это было?", 
  "Если бы магический кристалл мог открыть правду, о чем бы ты узнал?", 
  "Есть ли что-то, что ты давно мечтаешь сделать?", 
  "Самое большое достижение в твоей жизни?", 
  "Что в дружбе для тебя наиболее ценно?", 
  "Какое твое самое дорогое воспоминание?",  
  "Что для тебя значит дружба?", 
  "Какую роль любовь играет в твоей жизни?",  
  "Продолжите фразу: «Я бы хотел, чтобы был кто-то, с кем можно разделить…»", 
  "Поделитесь смущающим моментом из жизни.",  
  "Что ты ценишь в людях и почему?",   
  "Дом горит. Что спасешь (кроме живых существ, документов и денег)?", 
  "Что в этом году случилось впервые?", 
  "Какие качества ты любишь и ненавидишь в себе?", 
  "Что для Вас значит слово успех(не считая деньги)?", 
  "Что бы вы сказали себе 15-летнему?", 
  "О чём вы можете говорить часами?", 
  "Какой лучший совет Вам давали?", 
  "Без чего не проживаете ни дня?", 
  "Если бы пришлось есть одно блюдо всю жизнь, что это было бы?", 
  "Твой «Бесполезный талант»?", 
  "Что популярно, но тебя бесит?", 
  "Место, которое разочаровало? И куда хочешь вернуться?", 
  "Роли в зомби-апокалипсисе: лидер, предатель, первая жертва. Кто ты?", 
  "100 млн долларов, но нельзя тратить на себя. Куда денешь?", 
  "Путешествие во времени, у тебя только 1 час (можно только смотреть). Куда отправишься?", 
  "Кем мечтал стать в 7 лет?", 
  "За что тебя выгоняли из класса?", 
  "Месяц без смартфона за миллион?", 
  "Кот или собака? Продай мне выбор.", 
  "Твое самое непопулярное мнение? (Например: 'Я ненавижу сериал Друзья')", // ВОТ ЗДЕСЬ БЫЛА ОШИБКА
  "Какую самую глупую вещь ты купил, поддавшись рекламе?", 
  "Какую песню ты включаешь, когда никто не слышит, и тебе за нее стыдно?", 
  "Если бы ты мог отменить один любой закон (физики или юридический), что бы это было?", 
  "Какое качество в людях ты считаешь самым переоцененным? Почему?", 
  "Какой праздник нужно отменить навсегда?", 
  "Какой сериал все обожают, а ты терпеть не можешь?", 
  "Твоя самая бесполезная покупка за последний год?", 
  "Какое реалити-шоу — твой «тайный грех»?", 
  "Твой самый дурацкий способ потратить время в интернете?", 
  "Какую детскую привычку ты сохранил/а до сих пор?", 
  "Какую суперсилу ты бы выбрал/а для бытовых нужд (например, мгновенная сушка волос)?", 
  "Если бы ты мог/ла переехать в любую страну завтра, куда бы точно НЕ поехал/а? Почему?", 
  "Что ты выберешь: всегда говорить правду или всегда только врать?", 
  "Если бы тебе платили за хобби, чем бы ты занимался/лась?", 
  "Какую профессию ты бы освоил/а, если бы у тебя была вторая жизнь?", 
  "Твой «анти-талант»? То, что у тебя получается хуже всего.", 
  "Кем бы ты был/а в банде грабителей банков (водитель, мозг, хакер или тот, кто всех отвлекает)?", 
  "Если бы ты мог/ла удалить одно мобильное приложение из мира навсегда, что это было бы?", 
  "В какой исторической эпохе ты бы точно не выжил/а?", 
  "Какой один закон ты бы ввел/а для всех жителей Земли на один день?", 
  "Какое блюдо ты считаешь самым переоцененным в мире?", 
  "Если бы можно было оставить только одно приложение на телефоне, что бы это было?", 
  "Искусственный интеллект нас спасет или погубит?", 
  "Какую вещь ты хранишь годами, хотя она тебе не нужна?", 
  "Что ты выберешь: жить в центре в коробке или в огромном доме за городом?", 
  "Олл-инклюзив в Турции или поход с рюкзаком по Исландии?", 
  "Самый ужасный сосед в самолете — это кто?", 
  "Если бы ты мог/ла жить в любой эпохе, кроме нынешней, что бы ты выбрал/а?", 
  "Твой самый большой культурный шок в другой стране?", 
  "Ты бы согласился/лась на полет в один конец на Марс? Почему?", 
  "Какое качество в незнакомых людях тебя подкупает мгновенно?", 
  "Какого актера/актрису ты бы выбрал/а на роль себя в фильме про себя?", 
  "Если бы ты мог/ла украсть один талант у любого человека, что бы это было?", 
  "Если бы ты мог/ла изменить один финал в любом известном фильме/книге, какой именно?", 
  "Какое решение в твоей жизни было самым сложным, но принесло больше всего свободы?",
  "Что в твоем понимании значит 'прожить день не зря', если не считать работу и продуктивность?",
  "Какое качество в людях тебя пугает, потому что ты видишь его в самом/самой себе?",
  "Если бы ты писал/а автобиографию, какому человеку ты бы посвятил/а целую главу, хотя вы почти не общаетесь?",
  "Твой самый нелепый способ потратить деньги, о котором ты ни капли не жалеешь?",
  "Если завтра интернет исчезнет навсегда, какая профессия станет твоим планом Б?",
  "Что самое странное ты находил/а в своей сумке или кармане спустя долгое время?",
  "Если бы ты мог/ла переозвучить одного персонажа в известном фильме, кто бы это был и почему?",
  "В какой момент своей жизни ты почувствовал/а себя настоящим 'Главным героем', а не массовкой?",
  "Если бы твоя жизнь была саундтреком, какой трек играл бы в моменты твоих триумфов?",
  "Какую 'тайну' о Варшаве ты узнал/а недавно, и она изменила твое отношение к городу?",
  "Твой личный 'код доступа' — какие 3 темы нужно затронуть, чтобы ты сразу почувствовал/а связь с человеком?",
  "Если бы ты мог/ла стать невидимым/ой на один день, в какую дверь ты бы вошел/ла первым делом?",
  "Какую одну вещь ты бы хотел/а изменить в том, как люди общаются друг с другом сегодня?",
  "За что в своей жизни ты готов/а сказать 'спасибо' своим врагам или неудачам?",
  "Если бы ты мог/ла выбрать себе любое историческое прозвище (как 'Великий' или 'Мудрая'), каким бы оно было?",
  "Как ты считаешь, какая из современных технологий через 50 лет будет казаться людям самой нелепой?",
  "Если бы ты мог/ла заглянуть в будущее на 10 минут, какой один вопрос ты бы задал/а поисковику из 2050 года?",
  "Какую одну новость из будущего ты бы хотел/а увидеть в заголовках газет завтра утром?",
  "Если бы ты мог/ла навсегда стереть из памяти один фильм, чтобы посмотреть его заново как в первый раз, что бы это было?", 
  "Если бы твоя жизнь была фильмом, какой жанр это был бы и кто бы написал к нему саундтрек?",
  "Если бы ты мог/ла изменить одно событие в истории человечества, зная, что это изменит и твою нынешнюю жизнь?",
  "Если бы ты мог/ла изменить одно событие в истории человечества, зная, что это изменит и твою нынешнюю жизнь?",
  "Если бы ты мог/ла выбрать себе суперспособность, которая работала бы только ночью, что бы это было?",
  "Какой 'баг' в системе человеческого общения тебя бесит больше всего?",
  "Если бы ты был/а брендом, какой был бы твой главный лозунг?",
  "Какое самое смелое решение ты принял/а, когда все вокруг говорили тебе 'не делай этого'?",
  "В какой мировой заговор тебе чертовски хочется верить, даже если это звучит безумно?",
  "Если бы ты узнал/а, что твоя жизнь — это симуляция, какой первый вопрос ты бы задал/а 'программисту'?",
  "Какую историческую личность ты бы вызвал/а на допрос, чтобы узнать правду об одном конкретном событии?",
  "Какая фраза или слово мгновенно делает человека в твоих глазах 'своим'?",
  "Если бы ты мог/ла 'удалить' один тип поведения у всех людей в мире, что бы это было?",
  "Если бы ты мог/ла 'удалить' один тип поведения у всех людей в мире, что бы это было?",
  "Какая вещь из твоего дома больше всего похожа на артефакт из будущего?",
  "Что в современной культуре кажется тебе самым переоцененным 'трендом'?",
  "Если бы ты мог/ла отправить одно SMS всему миру одновременно, что бы в нем было написано?",
  "Веришь ли ты, что технологии делают нас ближе, или они просто создают иллюзию связи?",
  "Какую детскую мечту ты исполнил/а совсем недавно, будучи взрослым?",
  "Самый странный совет, который ты когда-либо получал/а и который неожиданно сработал?",
  "Какое слово ты используешь чаще всего, когда нервничаешь?",
  "Если бы у тебя был безлимитный бюджет на одну безумную покупку, что бы это было?",
  "За какой один момент в своей жизни тебе хочется поднять бокал прямо сейчас?"
];

// Блок вопросов для тематических ужинов Talk and Toast
const THEMATIC_QUESTIONS_POOL = [
  "Григорий Распутин: Как обычный мужик мог выжить после яда, четырех пуль и ледяной воды? В чем был его секрет?",
  "Если бы ты мог/ла открыть одну запечатанную дверь в истории (например, секретные архивы Ватикана), что бы ты надеялся/лась там найти?",
  "Как ты думаешь, какая археологическая находка была намеренно скрыта от человечества, чтобы не разрушить привычную картину мира?",
  "Если бы пирамиды Гизы оказались не гробницами, а техническими устройствами, какая версия их предназначения тебе ближе?",
  "Какая историческая личность кажется тебе самой большой мистификацией (человеком, которого на самом деле не существовало)?",
  "Где, по-твоему, спрятано 'Золото нацистов' или 'Янтарная комната', и почему их до сих пор не нашли?",
  "Веришь ли ты, что атланты — это не миф, а развитая цивилизация, следы которой мы видим повсюду, но не узнаем?",
  "Какое историческое событие ты бы хотел/а увидеть своими глазами, чтобы раз и навсегда понять: это был заговор или случайность?",
  "Существует ли артефакт, который ты бы побоялся/лась трогать, даже если бы он лежал перед тобой?",
  "Какую страницу из учебника истории ты бы вырвал/а, считая её на 100% ложной?",
  "Какой скрытый символ на долларовой купюре или в логотипе известного бренда кажется тебе самым подозрительным?",
  "Как ты думаешь, сколько шедевров в мировых музеях на самом деле являются гениальными подделками, которые эксперты боятся разоблачить?",
  "Если бы ты был/а художником-мистификатором, какой секретный знак ты бы оставлял/а на каждой своей работе?",
  "Веришь ли ты, что великие писатели прошлого (например, Шекспир) были лишь 'лицом' для группы анонимных авторов?",
  "Какая песня или картина кажется тебе зашифрованным посланием, которое мы еще не расшифровали?",
  "Считаешь ли ты современное абстрактное искусство формой массовой мистификации, где 'король голый'?",
  "Можно ли считать 'дипфейком' ту версию себя, которую мы создаем в соцсетях?",
  "Сможешь ли ты отличить близкого от его безупречной симуляции, или предпочтешь верить в красивую ложь?",
  "Веришь ли ты, что интернет — это 'мертвый лес', где 90% контента создано ботами для манипуляции нашим мнением?",
  "Какую новость за последний год ты до сих пор считаешь умелой постановкой или спецэффектом?",
  "Если бы ты мог/ла создать идеальный дипфейк любого человека, чтобы восстановить справедливость, чье лицо ты бы выбрал/а?",
  "Считаешь ли ты, что мы уже живем в симуляции, и дежавю — это просто баг при обновлении системы?",
  "Какой 'сумасшедший' заговор из интернета кажется тебе подозрительно логичным?",
  "Веришь ли ты в существование 'Мирового правительства' или хаос правит миром на самом деле?",
  "Если бы тайное общество предложило тебе вступить в свои ряды в обмен на молчание, какой секрет стал бы твоим вступительным взносом?",
  "Для чего на самом деле нужен адронный коллайдер (CERN): наука или попытка открыть портал в другое измерение?",
  "Какая корпорация, по-твоему, владеет миром больше, чем любое государство?",
  "Веришь ли ты, что Лунная программа была снята в павильоне, или это самая живучая мистификация в истории?",
  "Существует ли '25-й кадр' в современной рекламе, который заставляет нас покупать вещи, которые нам не нужны?",
  "Какое исчезновение самолета или корабля (как в Бермудском треугольнике) ты считаешь операцией по перемещению людей?",
  "Если бы завтра объявили о контакте с пришельцами, ты бы поверил/а в это или счел/ла бы это способом отвлечь внимание от кризиса?",
  "Какой секретный эксперимент над людьми (как MK-Ultra) проводится прямо сейчас, по твоему мнению?",
  "Кто на самом деле был Джеком Потрошителем: хирург, принц или кто-то, кого стерли из истории?",
  "Загадка перевала Дятлова: это был секретный запуск ракеты или испытание звукового оружия?",
  "Кто на самом деле создал Биткоин: Сатоши Накамото — это человек или искусственный интеллект спецслужб?",
  "Почему на картине 'Тайная вечеря' ноты, спрятанные в руках и хлебе, складываются в реквием?",
  "Багдадская батарейка: если у древних было электричество, почему они его 'забыли'?",
  "Существовал ли 'Нулевой пациент' на самом деле, или это всегда часть чьего-то бизнес-плана?",
  "Майкл Джексон: Он инсценировал свою смерть, чтобы наконец-то обрести покой и уйти от долгов и преследований?",
  "Масоны и Иллюминаты: Если они не правят миром, то зачем их символы напечатаны на каждой долларовой купюре?",
  "Эффект Манделы (Монополия): Когда именно у человечка из 'Монополии' исчез монокль, и почему мы все помним его с ним?",
  "5G и вышки: Связь будущего или инструмент для контроля эмоционального состояния толпы?",
  "Ковчег Завета: Он спрятан в Эфиопии, под Храмовой горой или его уже давно забрали те, кто его создал?",
  "Сигнал 'Wow!' из космоса в 1977-м — это приветствие или предупреждение, которое мы проигнорировали?",
  "Железная маска: Кем был таинственный узник Бастилии — братом-близнецом короля или его настоящим отцом?"
];

const SPEAKERS_HISTORY: Record<number, number> = {};

// В src/index.ts

const addPromoWizard = new Scenes.WizardScene(
  'ADD_PROMO_SCENE',
  async (ctx) => {
    await ctx.reply('Введите промокод (одним словом, например: FREE_FRIDAY):');
    return ctx.wizard.next();
  },
  async (ctx) => {
    (ctx.wizard.state as any).code = ctx.message.text.toUpperCase();
    await ctx.reply('ID игры, на которую действует код (или 0, если на любую):');
    return ctx.wizard.next();
  },
  async (ctx) => {
    (ctx.wizard.state as any).eventId = parseInt(ctx.message.text);
    await ctx.reply('Количество билетов (макс. использований):');
    return ctx.wizard.next();
  },
  async (ctx) => {
    const state = ctx.wizard.state as any;
    const max = parseInt(ctx.message.text);
    
    await db.insert(schema.promoCodes).values({
      code: state.code,
      type: 'free',
      maxUses: max,
      eventId: state.eventId === 0 ? null : state.eventId,
      // Можно добавить шаг с датой, но для простоты — +3 дня от текущей
      expiresAt: DateTime.now().plus({ days: 3 }).toJSDate() 
    });

    await ctx.reply(`✅ Промокод ${state.code} на ${max} билетов создан!`);
    return ctx.scene.leave();
  }
);

const STOCK_QUESTIONS = [
{
    question: "Война между Англией и Францией вошла в историю как «Столетняя война». Однако историки знают, что на самом деле конфликт длился с перерывами дольше. Сколько лет шла Столетняя война?",
    hints: [
      "1. Это атомный номер Ливермория — одного из самых тяжелых элементов таблицы Менделеева.",
      "2. Если бы война продлилась ещё 4 года, то составила бы ровно 12 десятилетий.",
      "3. Это число на 16 лет больше, чем «век»." ],
    answer: "116",
    fact: "На самом деле это была не одна непрерывная война, а серия из четырех крупных конфликтов, разделенных перемириями. Название «Столетняя война» придумали гораздо позже — только в XIX веке, чтобы как-то объединить этот бесконечный сериал из сражений в один учебник."
  },
{
    question: "В игре Minecraft каждый предмет в инвентаре занимает слот. Большинство предметов (блоки, еда) собираются в так называемый «стак» (stack). Какое максимальное количество предметов помещается в один стак?",
    hints: [
      "1. Именно столько клеток на стандартной шахматной доске.",
      "2. Это число бит в названии культовой приставки Nintendo (Nintendo ...)",
      "3. Это 2 в шестой степени"
    ],
    answer: "64",
    fact: "Число 64 удобно для двоичного кода компьютеров (2^6), поэтому Нотч (создатель игры) выбрал именно его."
  },
{
    question: "Модель игровой консоли от Microsoft, вышедшая в 2005 году (Xbox ...).",
    hints: [
      "1. Сумма внутренних углов любого четырёхугольника (квадрата или прямоугольника)",
      "2. Три раза по 120.",
      "3. Это число обозначает полный цикл. Столько градусов составляет полный оборот вокруг своей оси."
    ],
    answer: "360",
    fact: "В 2005 году Sony готовилась выпустить PlayStation 3. В Microsoft резонно рассудили, что если они назовут свою консоль «Xbox 2», то в глазах обычного покупателя она будет выглядеть «старее» или слабее, чем «третья» плойка."
  },
{
    question: "Самая известная модель джинсов в мире от Levi’s. Она появилась в 1890 году и стала стандартом «пятикарманных» джинсов. Какой номер у этой модели?",
    hints: [
      "1.  Это код ошибки HTTP «Not Implemented» (Не реализовано).",
      "2.  Это название легиона клонов, которым руководил Энакин Скайуокер (Вейдер) в «Звёздных войнах».",
      "3. Сумма всех цифр этого числа равна 6."
    ],
    answer: "501",
    fact: "Маленький пятый кармашек (watch pocket) изначально предназначался для карманных часов, а не для монет."
  },
{
    question: "Земля поделена на часовые пояса. Теоретически их должно быть 24. Но из-за политических игр (где-то добавляют полчаса, где-то 45 минут) их больше. Сколько всего часовых поясов используется сейчас в мире? (Ближайшее целое число).",
    hints: [
      "1. Это калибр популярного полицейского револьвера (.Special), часто встречающийся в фильмах нуар.",
      "2. Столько 'попугаев' составляла длина удава в знаменитом советском мультфильме.",
      "3.  Это число равно 19 умножить на 2"
    ],
    answer: "38",
    fact: "Некоторые страны, как Непал, имеют пояс с разницей в 45 минут, чтобы не совпадать с Индией из принципа."
  },
{
    question: "Сколько томов в классическом школьном издании романа «Война и мир» Толстого?",
    hints: [
      "1. Количество благородных истин в буддизме.",
      "2. Атомный номер Бериллия.",
      "3. Столько всадников Апокалипсиса описано в Откровении Иоанна Богослова."
    ],
    answer: "4",
    fact: "Лев Толстой переписывал роман вручную 8 раз! А отдельные эпизоды (например, начало) — более 20 раз. Его жена, Софья Андреевна, была его главным «редактором» и переписывала эти тысячи страниц набело, разбирая его ужасный почерк при свечах."
  },
{
    question: "Это число считается «самым приятным» (nicest number) в интернете. Всякий раз, когда оно появляется в ленте, пользователи обязаны написать в комментариях «Nice». Назовите его.",
    hints: [
      "1. Это год проведения легендарного фестиваля Вудсток и год высадки человека на Луну (19...).",
      "2. Графически это число напоминает знак Инь и Ян (рыбки, плывущие по кругу).",
      "3. Популярная поза в Камасутре."
    ],
    answer: "69",
    fact: "Илон Маск специально ставил цену на модели Tesla (например, $69,420), чтобы повеселить интернет-сообщество."
  },
{
    question: "В книге «Автостопом по галактике» суперкомпьютер Deep Thought думал 7,5 миллионов лет, чтобы дать ответ на «Главный вопрос жизни, Вселенной и всего такого». Какое число он выдал?",
    hints: [
      "1. Это магическое число для оптики. Именно под таким углом (в градусах) свет преломляется в каплях воды, чтобы человеческий глаз мог увидеть радугу.",
      "2. Атомный номер Молибдена.",
      "3. В двоичной системе счисления это число выглядит как идеальный чередующийся ритм: 101010."
    ],
    answer: "42",
    fact: "Дуглас Адамс признался, что выбрал число случайно. Он просто хотел «обычное, небольшое число» и посмотрел в окно сада."
  },
{
    question: "На этикетке самого популярного кетчупа в мире «Heinz» красуется число, которое якобы обозначает количество сортов продукции, хотя на момент утверждения дизайна сортов было уже больше 60. Сам Генри Хайнц считал это число счастливым. Назовите его.",
    hints: [
      "1. Именно столько пассажиров (вместе с террористами) захватил герой Уэсли Снайпса в боевике 1992 года.",
      "2. В этом году (19...) Советский Союз запустил первый искусственный спутник Земли, начав космическую эру.",
      "3. : Это число — часть названия популярной российской рэп-группы «Каста», если вспомнить их трек про «... регион»."
    ],
    answer: "57",
    fact: "Генри Хайнц увидел в поезде рекламу обуви «21 стиль» и решил, что число 57 звучит солиднее."
  },
{
    question: "В США телефонные номера, начинающиеся с этого трехзначного кода, являются бесплатными для звонящего (toll-free). Компания платит за входящий звонок. Что это за код?",
    hints: [
      "1. Это номер модели Терминатора (Т-...), которого сыграл Арнольд Шварценеггер.",
      "2. Столько лет правила самая долгая династия в Китае — Чжоу.",
      "3. В лёгкой атлетике это одна из самых сложных дистанций среднего бега — ровно два полных круга по стандартному стадиону."
    ],
    answer: "800",
    fact: "Из-за огромной популярности этого кода в США закончились комбинации номеров, и властям пришлось вводить дополнительные коды (888, 877, 866), но этот — самый первый и престижный."
  },
{
    question: "В романе-антиутопии Джорджа Оруэлла «1984» существует страшная комната пыток, в которой заключённый сталкивается со своим самым главным страхом. Назовите номер этой комнаты.",
    hints: [
      "1. В американских университетах этим номером обозначают вводные курсы для новичков (например, «Психология ...»).",
      "2. Именно столько пятнистых щенков фигурирует в названии известного мультфильма Диснея.",
      "3. Это число — палиндром из нолей и единиц."
    ],
    answer: "101",
    fact: "Сам Оруэлл назвал комнату 101 в честь зала заседаний на своей работе на BBC, где ему приходилось сидеть на скучных собраниях."
  },
{
    question: "У человека 46 хромосом. А сколько хромосом у обычной домашней кошки?",
    hints: [
      "1.  Параллель, по которой была разделена Северная и Южная Корея после войны.",
      "2. Количество слотов (кармашков) на колесе американской рулетки.",
      "3. Это название одного из самых известных калибров револьверов в мире. Именно с таким оружием в кобуре ходили почти все американские полицейские и детективы в фильмах нуар."
    ],
    answer: "38",
    fact: "У курицы 78 хромосом, а у папоротника — больше 1200. Количество хромосом не говорит о сложности организма."
  },
{
    question: "Сколько Оскаров получил фильм «Властелин колец: Возвращение короля»? (Это рекорд, который делят с Титаником и Бен-Гуром).",
    hints: [
      "1. Номер миссии Аполлон, которая первой села на Луну.",
      "2. Количество игроков одной футбольной команды на поле.",
      "3. Первое двузначное простое число."
    ],
    answer: "11",
    fact: "Фильм совершил так называемый «чистый размах» (Clean Sweep). Он был номинирован в 11 категориях и выиграл во всех 11. Для сравнения: у «Титаника» было 14 номинаций (3 проигрыша), а у «Бен-Гура» — 12 номинаций (1 проигрыш)."
  },
{
    question: "Сколько клавиш (черных и белых вместе) на стандартном классическом фортепиано?",
    hints: [
      "1. Именно до такой скорости (в милях в час) нужно было разогнать DeLorean в фильме «Назад в Будущее», чтобы переместиться во времени.",
      "2. Это число используется радикальными группировками как кодовое приветствие (H.H.), из-за чего его часто банят в никнеймах игр.",
      "3. Визуально это выглядит как две бесконечности, поставленные вертикально."
    ],
    answer: "88",
    fact: "Существует фирма Bösendorfer, которая делает рояли модели «Imperial» с 97 клавишами. Лишние клавиши в самом низу полностью выкрашены в черный цвет, чтобы пианист не запутался. Большинство музыкантов их никогда не нажимают, но они нужны для резонанса — благодаря им весь рояль звучит глубже и богаче."
  },
{
    question: "В колоде карт Uno много цветных карт, но сколько всего карт в стандартной классической колоде (включая джокеров и нулевки)?",
    hints: [
      "1. В сериале «Остаться в живых» (Lost) героям нужно было вводить код в компьютер каждые ... минут, чтобы спасти мир.",
      "2. Это священное число в буддизме и индуизме (столько бусин в четках для мантр).",
      "3. Атомная масса серебра (округленно)."
    ],
    answer: "108",
    fact: "Официальный производитель Uno (компания Mattel) в 2019 году сделала шокирующее заявление в Твиттере: Вы НЕ имеете права класть «+4» на «+2» или «+4» на «+4»! Весь мир десятилетиями играл в «переводного», создавая цепочки, которые заставляли последнего игрока брать полколоды, но по правилам это запрещено. Игроки в комментариях просто послали компанию подальше, заявив: «Мы купили игру, мы и решаем, как в неё играть»."
  },
{
    question: "Легендарная «Мать дорог» в США, одно из первых шоссе, соединивших Чикаго и Лос-Анджелес. Про неё поют песни, снимают фильмы (даже в «Тачках» она есть). Какой номер у этой трассы?",
    hints: [
      "1. Это число джедаев, которые подлежали уничтожению согласно секретному приказу Императора Палпатина («Выполнить Приказ...»).",
      "2.  Последние две цифры года, когда Англия единственный раз выиграла Чемпионат мира по футболу.",
      "3. Если из «Числа зверя» вычесть 600, получится ответ."
    ],
    answer: "66",
    fact: "Дорога официально выведена из системы шоссе США в 1985 году, но туристический поток по ней только растет."
  },
{
    question: "Сатурн знаменит своими кольцами, а Плутон тем, что его выгнали из планет. А сколько спутников у Марса?",
    hints: [
      "1. Атомный номер Гелия.",
      "2. Это единственное чётное простое число в математике.",
      "3.  Столько башен было у Всемирного торгового центра в Нью-Йорке."
    ],
    answer: "2",
    fact: "Спутники зовут Фобос и Деймос (Страх и Ужас). Джонатан Свифт предсказал их наличие за 150 лет до открытия телескопами."
  },
{
    question: "Какое максимальное количество символов изначально разрешал Твиттер в одном посте?",
    hints: [
      "1. Это стандартный темп (ударов в минуту) для жанров музыки дабстеп и транс.",
      "2. Уровень IQ, который часто называют порогом 'гениальности'.",
      "3. Максимальная скорость на польских автомагистралях (Autostrada) в км/ч."
    ],
    answer: "140",
    fact: "Это число было выбрано не случайно, а из-за технических ограничений SMS-сообщений. В стандартном SMS было 160 символов, и создатели Твиттера «откусили» 20 из них под имя пользователя и метаданные."
  },
{
    question: "Сколько специй и трав входит в секретный оригинальный рецепт полковника Сандерса (KFC)?",
    hints: [
      "1. Именно столько лет длится стандартный цикл солнечной активности.",
      "2. Прозвище главной героини сериала «Очень странные дела».",
      "3. Выглядит как две барабанные палочки."
    ],
    answer: "11",
    fact: "Оригинальный рукописный рецепт 1940-х годов хранится в цифровом сейфе в штаб-квартире в Луисвилле. Сейф окружен датчиками движения и бетоном толщиной в полметра. Даже генеральный директор компании не знает рецепта целиком."
  },
{
    question: "Рэй Брэдбери назвал свой роман-антиутопию в честь температуры, при которой (по его мнению) воспламеняется и горит бумага. Назовите это число.",
    hints: [
      "1. Это код ошибки HTTP, который означает «Недоступно по юридическим причинам» (блокировка цензурой) — очень символично для этой книги.",
      "2.  Сумма цифр этого числа равна 10.",
      "3. Эта температура указана по шкале Фаренгейта. Если перевести её в привычные нам градусы Цельсия, получится примерно 233°. °F = (°C × 9/5) + 32."
    ],
    answer: "451",
    fact: "Брэдбери ошибся. Бумага самовоспламеняется при 450 градусах Цельсия, а по Фаренгейту это было бы около 842. Но «451» звучит красивее."
  },
{
    question: "Согласно самому популярному (хотя и научно спорному) тесту личности в мире — типологии Майерс-Бриггс (MBTI) — всё многообразие человеческих характеров можно разделить на строго определенное количество типов. Какое это число?",
    hints: [
      "1. Именно такой порядковый номер носил французский король Людовик, который потерял голову на гильотине во время Великой французской революции.",
      "2. В англоязычной культуре существует понятие «Sweet ...» — это возраст совершеннолетия, когда девушкам устраивают пышные балы, а подросткам в США разрешают официально сесть за руль.",
      "3. Это число является «идеальным квадратом» и четвертой степенью двойки."
    ],
    answer: "16",
    fact: "Самое удивительное, что создательницы самого популярного теста в мире — Кэтрин Бриггс и её дочь Изабель Майерс — не имели психологического образования. Они разработали систему из 16 типов во время Второй мировой войны, чтобы помочь женщинам, которые впервые выходили на работу вместо ушедших на фронт мужчин, быстрее найти подходящую им сферу деятельности."
  },
{
    question: "Именно так называется второй студийный альбом британской певицы Адель (Adele), который принес ей мировую славу и 6 премий «Грэмми».",
    hints: [
      "1. Согласно легендарному эксперименту доктора Макдугалла, именно столько граммов теряет тело человека в момент смерти. Это число вошло в историю как «вес человеческой души».",
      "2. Это «самый взрослый» возраст совершеннолетия. Только по достижении этого возраста в США разрешено официально покупать алкоголь и заходить в казино Лас-Вегаса.",
      "3. В азартных играх это число — символ победы, а в математике — это сумма всех чисел на гранях стандартного игрального кубика."
    ],
    answer: "21",
    fact: "Этот альбом был вдохновлен болезненным разрывом Адель с её парнем. Но самое забавное случилось после того, как '21' стал мировым хитом и собрал все возможные награды. Бывший возлюбленный певицы позвонил ей и на полном серьезе потребовал долю от прибыли!"
  },
{
    question: "Этот год стал переломным для всей Европы. В Париже начались студенческие протесты под лозунгом «Запрещать запрещено», а в Праге — попытка построить «социализм с человеческим лицом». О каком числе (годе) идет речь?",
    hints: [
      "1. Одно из самых популярных названий для компактных механических клавиатур — это именно это число (столько клавиш в таком форм-факторе). Считается «золотой серединой» между удобством и размером.",
      "2. Если на американском термометре вы увидите это число по Фаренгейту, не пугайтесь — это идеальная комнатная температура, которая в привычных нам градусах Цельсия равна ровно 20°.",
      "3. Если вы перевернете это число вверх ногами (на 180 градусов), то получите число 89."
    ],
    answer: "68",
    fact: "Именно в 1968 году произошло событие, которое историки называют 'Матерью всех демонстраций'. Дуглас Энгельбарт впервые показал миру компьютерную мышь, гипертекст, окна и даже видеоконференцию! Представьте, на дворе 68-й, люди только-только привыкли к цветному ТВ, а кто-то уже кликал мышкой по экрану. Этот год буквально 'запрограммировал' наше будущее!"
  },
{
    question: "Этот год в XX веке называют «Летом любви». В этом же году медицина сделала невозможное — человек впервые получил сердце другого человека. О каком числе (годе) идет речь?",
    hints: [
      "1. Именно в этом году группа The Beatles выпустила свой самый знаменитый альбом «Sgt. Pepper’s Lonely Hearts Club Band», который полностью изменил историю музыки.",
      "2. Это число часто вызывает вздох у жителей Европы. Именно такой возраст установлен как стандартный для выхода на пенсию во многих странах (например, в Германии, Норвегии и Исландии).",
      "3. Именно этот номер носит знаменитая комета Чурюмова — Герасименко, на которую человечество впервые в истории посадило космический аппарат (зонд «Фила»). В новостях её часто называли «комета-уточка» из-за необычной формы."
    ],
    answer: "67",
    fact: "1967 год стал пиком культуры хиппи. Летом того года около 100 000 человек собрались в Сан-Франциско, чтобы праздновать любовь и свободу. Именно тогда песня 'All You Need Is Love' стала мировым гимном. Интересно, что в том же году в США впервые ввели обязательную маркировку сигарет предупреждением о вреде здоровья. Мир одновременно учился спасать сердца и заботиться о легких!"
  },
{
    question: "Какое число объединяет «золотой год» мирового кинематографа, запуск легендарной игровой приставки PlayStation и самый опасный радиоактивный элемент в ядерной физике?",
    hints: [
      "1. Именно в этом году (19...) вышли фильмы, ставшие абсолютной классикой: «Король Лев», «Криминальное чтиво», «Форрест Гамп» и «Побег из Шоушенка». Кинокритики до сих пор спорят, был ли в истории год круче этого.",
      "2. Это атомный номер Плутония. Именно этот элемент был «сердцем» первой в мире испытанной ядерной бомбы и той, что была сброшена на Нагасаки.",
      "3. Это число на шесть меньше, чем сто. Или результат умножения 47 на 2."
    ],
    answer: "94",
    fact: "В декабре 1994 года в Японии произошло событие, которое похоронило доминирование Nintendo и Sega. На прилавках появилась первая Sony PlayStation. Мало кто знает, что изначально Sony и Nintendo должны были выпустить приставку вместе, но в последний момент Nintendo 'кинула' партнеров. Sony так разозлилась, что решила выпустить свою консоль в одиночку — и в итоге изменила мир гейминга навсегда, сделав 94-й год точкой отсчета эпохи 3D!"
  },
{
    question: "Это число описывает нашу планету лучше любого другого. Оно же стало символом невероятного долголетия на троне в современной истории. О каком числе речь?",
    hints: [
      "1. Именно столько ударов в минуту составляет средний пульс здорового взрослого человека в состоянии полного покоя.",
      "2. Первые две цифры в названии легендарного самолета. Именно эта машина в 50-х годах сделала реактивные перелеты доступными для обычных людей и изменила мир навсегда.",
      "3. В инвестициях существует «Правило ...». Если вы разделите это число на годовую процентную ставку, то получите примерное количество лет, через которое ваш капитал удвоится."
    ],
    answer: "70",
    fact: "Мы привыкли говорить, что воды на Земле — 70%, и кажется, что её бесконечно много. Но вот в чём подвох: если собрать всю-всю воду планеты (включая океаны и ледники) в один шар, его диаметр составит всего около 1385 километров. Это меньше, чем расстояние от Варшавы до Неаполя! Из этих 70% поверхности лишь крошечная доля (менее 1%) — это доступная нам пресная вода. Так что мы живём на очень 'мокрой' планете, где почти нечего пить!"
  },
{
    question: "Это число описывает количество «букв» в коде жизни, количество фундаментальных сил, управляющих Вселенной.",
    hints: [
      "1. Весь генетический код любого живого существа на Земле — от бактерии до человека — написан «алфавитом», состоящим ровно из стольки азотистых оснований.",
      "2. В науке считается, что во Вселенной существует ровно столько фундаментальных взаимодействий (сил): гравитация, электромагнетизм, сильное и слабое ядерные взаимодействия.",
      "3. Если рассматривать время как полноценное измерение, то мы с вами живем в физическом мире, который имеет ровно столько измерений."
    ],
    answer: "4",
    fact: "А вы знали, что название легендарной игры 'Тетрис' напрямую связано с этим числом? Алексей Пажитнов придумал его от греческого слова 'тетра', что означает 'четыре'. Весь секрет игры в том, что абсолютно каждая фигурка в Тетрисе (как бы она ни была изогнута) состоит ровно из 4 квадратиков."
  },
{
    question: "Это число — главный символ мимолетного успеха и самая популярная «доля» в мировом летоисчислении. О каком числе идет речь?",
    hints: [
      "1. В большом теннисе это «первая ступень» в счете. Если вы выиграли первый мяч в гейме, судья объявит именно это число (счет станет «... — ноль»).",
      "2. Знаменитый художник Энди Уорхол предсказал, что в будущем у каждого человека будет именно столько «минут славы».",
      "3. Это количество шаров, которые выставляются в пирамиду в начале игры в «Бильярд» (Пул или Русская пирамида)."
    ],
    answer: "15",
    fact: "В 1968 году Энди Уорхол сказал: 'В будущем каждый сможет стать всемирно известным на 15 минут'. Тогда это казалось абсурдом, но сегодня, в эпоху TikTok, Reels и Shorts, это стало реальностью. Вирусное видео может сделать обычного человека звездой мирового масштаба ровно на этот короткий срок, пока лента не обновится. Уорхол не просто угадал число, он описал психологию современного интернета за 40 лет до его появления!"
  },
{
    question: "Это число диктует правила в казино Лас-Вегаса, ритм нашей жизни в течение года и даже устройство классического фортепиано. О каком числе идет речь?",
    hints: [
      "1. Это число стоит в названии самого легендарного американского стратегического бомбардировщика — B-... Stratofortress. Этот самолет настолько надежен, что на нем летали деды, летают отцы и будут летать внуки.",
      "2. Под таким именем известен «самый одинокий кит в мире». Он поет на частоте ... Гц, которая гораздо выше, чем у других китов, поэтому сородичи его просто не слышат.",
      "3. Это название знаменитого «горящего» коктейля из трех слоев: кофейного ликера, сливочного ликера и апельсинового ликера (Cointreau). Его принято пить залпом через трубочку."
    ],
    answer: "52",
    fact: "Знаете, почему число 52 — самое мистическое в математике? Количество способов перемешать колоду из 52 карт ($52!$) настолько огромно, что каждый раз, когда вы по-настоящему хорошо тасуете карты, вы создаете комбинацию, которой никогда не существовало в истории Вселенной."
  },
{
    question: "Британский антрополог Робин Данбар вычислил «когнитивный предел» человеческого мозга. Согласно его теории, существует максимальное количество людей, с которыми один человек может поддерживать стабильные социальные связи. Назовите это число.",
    hints: [
      "1. Это число в миллионах километров — среднее расстояние от Земли до Солнца, известное в науке как одна «астрономическая единица» (1 AU).",
      "2. Считается, что это был средний размер деревни в эпоху неолита и типичная численность роты в армиях большинства стран мира.",
      "3. В мире ритма это число обозначает очень высокий темп (BPM) — именно с такой скоростью бьется бит в энергичном хардстайле или быстром техно."
    ],
    answer: "150",
    fact: "Антрополог заметил, что как только в группе становится больше 150 человек, люди перестают узнавать друг друга в лицо. Именно поэтому многие крупные компании (например, создатели ткани Gore-Tex) разделяют свои офисы на разные здания, как только штат в одном отделе превышает это «магическое» число."
  },
{
    question: "В мире драгоценных металлов существуют строгие стандарты чистоты — пробы. Пока весь мир охотится за «стерлинговым серебром» (925 пробы), этот трехзначный номер является самым популярным стандартом для высококачественных изделий и столового серебра в Восточной Европе. Назовите его.",
    hints: [
      "1. В мире инвестиций это число означает абсолютное доминирование: это ровно семь восьмых долей (7/8) от общего капитала в тысячу единиц.",
      "2. В индустрии гостеприимства это число — символ исключительного комфорта. Именно такую плотность плетения нитей (Thread Count) выбирают лучшие пятизвездочные отели мира для своего постельного белья.",
      "3. Это число ровно на 50 меньше, чем самая популярная в мире 925-я проба."
    ],
    answer: "875",
    fact: "875-я проба была «золотым стандартом» серебра в СССР. Большинство знаменитых изделий с чернью из Кубачи или винтажных ложек, которые хранятся во многих семьях, имеют именно это клеймо. Оно считается более долговечным, чем 925-я проба, так как чуть большее содержание меди делает металл тверже и устойчивее к царапинам."
  },
  {
    question: "Это число — «потолок» цифровой архитектуры. Оно является максимально возможным значением, которое можно записать в один стандартный компьютерный байт. Назовите его.",
    hints: [
      "1. В мире дизайна и цифрового искусства это число означает максимальную яркость: если в модели RGB выставить все три канала на этот максимум, вы получите идеально белый цвет.",
      "2. Это число — виновник легендарного бага в игре Pac-Man: когда игрок доходил до ... уровня, счетчик памяти «переполнялся» через это число, и игра превращалась в кашу из символов.",
      "3. Это максимально возможное значение для любого из четырех секторов обычного IP-адреса (например, 192.168.1....)."
    ],
    answer: "255",
    fact: "Почему именно 255? Компьютеры считают в двоичной системе (нули и единицы). Один байт состоит из 8 бит. Максимальное число, которое можно записать восемью единицами в двоичном коде ($11111111_2$), — это ровно 255. Если считать вместе с нулем, получается всего 256 значений. Именно поэтому в старых играх у героев часто было максимум 255 жизней или предметов."
  },
 {  
    question: "В американской поп-культуре это трехзначное число стало абсолютным синонимом слов «информация» или «свежие новости». Если кто-то хочет узнать все подробности ситуации или сплетни, он спрашивает: «What's the ...?». Назовите это число.",
    hints: [
      "1. Это легендарный номер телефонной справочной службы в США и Канаде, по которому десятилетиями звонили, чтобы узнать номер телефона или адрес любого человека или компании.",
      "2. Именно это число вынесено в заголовок дебютного альбома «королевы хип-хоп соула» Мэри Джей Блайдж, который в 1992 году изменил звучание всей современной R&B музыки.",
      "3. Математическая подсказка: если сложить цифры этого числа, получится 6, а если разделить само число на 3, вы получите 137."
    ],
    answer: "411",
    fact: "Термин «411» стал сленговым именно благодаря телефону. В эпоху до интернета это был самый быстрый способ получить нужную информацию («get the info»). В 90-х фраза «What's the 411?» (в значении «В чем суть? Что происходит?») стала настолько популярной, что её начали использовать во всем мире, даже там, где телефонная справка работала по другим номерам."
  },
{
    question: "Этот номер — символ невероятной стойкости. Он является атомным номером металла, который обладает самой высокой температурой плавления среди всех чистых элементов (3422°C). Именно благодаря ему нити накаливания в классических лампочках могут светить часами, не плавясь. Назовите это число.",
    hints: [
      "1. Первые две цифры «Королевы небес» — легендарного двухпалубного самолета Boeing, который первым в истории сделал дальние перелеты массовыми.",
      "2. В поп-культуре это число — первая часть названия меланхоличного хита 90-х группы The Connells про ностальгию («... - 75»).",
      "3. Это атомный номер Вольфрама — самого тугоплавкого металла на планете."
    ],
    answer: "74",
    fact: "Вольфрам (74) настолько тяжелый и плотный, что его вес почти идентичен золоту. Этим часто пользуются мошенники, изготавливая вольфрамовые слитки и покрывая их слоем золота — такую подделку невозможно отличить по весу, её выдает только рентген или глубокое сверление."
  },
  {
    question: "Это трехзначное число сегодня можно встретить на ногах миллионов людей по всему миру. Оно является названием одной из самых успешных моделей кроссовок от бренда из Бостона, которая вернула моду на ретро-эстетику 70-х. Назовите его.",
    hints: [
      "1. В автомобильном мире это число — имя легендарного двигателя V8 (объемом в столько же кубических дюймов), который был «сердцем» культовых Chevrolet Corvette и Camaro в их золотую эпоху.",
      "2. Узнаваемая фишка этой модели — агрессивная «шипованная» подошва, которая необычно заходит высоко на пятку. Этот дерзкий дизайн сделал их самыми узнаваемыми кроссовками в соцсетях за последние годы.",
      "3. Математическая подсказка: это число получится, если вы просто умножите 109 на 3."
    ],
    answer: "327",
    fact: "Кроссовки New Balance 327 стали настоящим феноменом: дизайнеры объединили в них черты сразу трех архивных моделей 1970-х годов. А в мире моторов двигатель «327 Small-Block» до сих пор считается одним из лучших агрегатов в истории General Motors за его надежность и невероятную мощность."
  },
  {
    question: "В мире глобального бизнеса и инвестиций это число — символ «обретения статуса». В бюрократической системе США это официальный номер формы (I-...), которую подают для регистрации постоянного места жительства и получения Green Card. Назовите его.",
    hints: [
      "1. В индустрии развлечений и шоу-бизнеса это число — часть названия протокола (DMX-...), который служит «нервной системой» для управления светом и спецэффектами на стадионных концертах мировых звезд.",
      "2. В американской урбанистике это число обозначает знаменитую скоростную магистраль-кольцо вокруг Шарлотта — одного из крупнейших финансовых центров США, где базируется Bank of America.",
      "3. Если взять количество дней в обычном году (365) и прибавить к ним ровно 120 (столько минут в двух часах), получится это число."
    ],
    answer: "485",
    fact: "Форма I-485 (Adjustment of Status) — это юридический мост к американскому гражданству. А её «тезка» в мире техники, протокол 485, настолько надежен, что именно на нем работают системы управления в небоскребах и на глубоководных буровых платформах по всему миру."
  },
  {
    question: "Это число — символ «переходного момента» в истории. В мире классической инженерии оно обозначает редкую модель-гибрид, которая выпускалась всего один год, чтобы соединить комфорт семейного авто с мощностью спортивного болида. Назовите его.",
    hints: [
      "1. География и космос: это телефонный код региона в США, известного как «Космическое побережье» (Space Coast). Именно оттуда, из зоны с этим кодом, человечество отправляет свои самые мощные ракеты к звездам.",
      "2. Математика времени: если вы решите взять отпуск на 47 полных недель, то ваша свобода продлится ровно столько дней.",
      "3. Интрига в деталях: в истории легендарного немецкого бренда из Мюнхена этот индекс носил эксклюзивный кабриолет, который был выпущен тиражом всего в 1011 экземпляров, что делает его одной из самых редких находок для инвесторов в ретро-кары."
    ],
    answer: "329",
    fact: "BMW 329 — это настоящая коллекционная редкость. Она появилась в 1937 году как результат смелого эксперимента: кузов от одной модели, а начинка от другой. В мире «стоков» и винтажа такие «переходные» модели ценятся выше всего, так как они фиксируют момент эволюции бренда до того, как он стал мировым гигантом."
  }
];

// --- 3. HELPERS ---
const encodeCat = (str: string) => Buffer.from(str).toString('base64').replace(/=/g, '');
const decodeCat = (str: string) => Buffer.from(str, 'base64').toString('utf-8');

const parseEventDesc = (desc: string | null) => {
  if (!desc) return { title: 'Мероприятие', address: 'Уточняется' };
  const parts = desc.split('###');
  return { title: parts[0].trim(), address: parts[1] ? parts[1].trim() : 'Секретная локация откроектся за 3 часа до начала 🔒' };
};

async function notifyNextInWaitlist(eventId: number, eventType: string, dateString: string) {
    const nextInLine = await db.query.bookings.findFirst({
        where: and(eq(schema.bookings.eventId, eventId), eq(schema.bookings.paid, false)),
        orderBy: [asc(schema.bookings.id)]
    });

    if (nextInLine) {
        const candidate = await db.query.users.findFirst({ where: eq(schema.users.id, nextInLine.userId) });
        if (candidate) {
            await bot.telegram.sendMessage(candidate.telegramId, 
                `🔥 Хорошие новости!\n\nНа игру "${eventType}" (${dateString}) освободилось место! 🥂\n\nСкорее переходи в "Игры", чтобы занять его!`, 
                { parse_mode: 'HTML' }).catch(() => {});
        }
    }
}



// --- 4. STATE ---

const PENDING_PAYMENTS = new Map<string, { time: DateTime, notified: boolean }>();
// Храним использованные вопросы для каждой конкретной игры (eventId -> Set вопросов)

// Храним использованные вопросы для каждой игры, чтобы не повторяться
const USED_TOPICS_BY_EVENT = new Map<number, Set<string>>();




const STOCK_STATE = {
  isActive: false,
  currentEventId: 0, // <--- Сюда бот запомнит ID игры после твоей команды
  currentQuestionIndex: -1,
  currentPhase: 0,
  playerAnswers: new Map<number, number>(), // userId -> ответ
  participants: new Map<number, { id: number, num: number, name: string }>() // userId -> инфо
};

const TALK_STATE = { currentFact: '', currentUser: '', isActive: false };

// --- 5. БОТ И СЦЕНЫ ---
const bot = new Telegraf<any>(process.env.TELEGRAM_BOT_TOKEN || '');

// 1. Мастер регистрации
const registerWizard = new Scenes.WizardScene(
  'REGISTER_SCENE',
  // --- ШАГ 1: Приветствие и запрос Имени ---
  async (ctx) => {
    await ctx.replyWithHTML(
      `👋 Почти готово!\n\n` +
      `Нужно внести тебя в базу Алгоритма, для этого пройди анкету из 3-х вопросов (всего 30 секунд). ` +
      `После анкеты возвращайся в "Игры" и покупай билет без помех!\n\n` +
      `1. Как тебя зовут? (пиши прям сюда своё имя)`
    );
    return ctx.wizard.next();
  },

  // --- ШАГ 2: Сохранение имени и запрос Возраста ---
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    (ctx.wizard.state as any).name = ctx.message.text;
    await ctx.reply('2. Сколько тебе полных лет? (Введи число)');
    return ctx.wizard.next();
  },

  // --- ШАГ 3: Сохранение возраста и запрос Пола ---
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    const age = parseInt(ctx.message.text);
    if (isNaN(age)) {
      return ctx.reply('❌ Ошибка! Пожалуйста, введи возраст цифрами (например: 25):');
    }
    (ctx.wizard.state as any).age = age.toString();
    await ctx.reply(
      '3. Твой пол (для баланса пар на играх):',
      Markup.keyboard([['Мужчина', 'Женщина']]).oneTime().resize()
    );
    return ctx.wizard.next();
  },

  // --- ШАГ 4: Нормализация пола, Сохранение в БД и Редирект ---
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    const input = ctx.message.text.trim(); // Убираем лишние пробелы

    let normalizedGender = '';
    if (input === 'Мужчина') normalizedGender = 'Мужчина';
    else if (input === 'Женщина') normalizedGender = 'Женщина';
    // На случай, если ввели вручную с маленькой буквы
    else if (input.toLowerCase().includes('муж')) normalizedGender = 'Мужчина';
    else if (input.toLowerCase().includes('жен')) normalizedGender = 'Женщина';

    if (!normalizedGender) {
      // Если не поняли ответ, кидаем клавиатуру СНОВА и НЕ убираем её (без oneTime)
      return ctx.reply(
        'Пожалуйста, выбери пол, нажав на одну из кнопок ниже 👇',
        Markup.keyboard([['Мужчина', 'Женщина']]).resize().persistent()
      );
    }

    const data = ctx.wizard.state as any;

    await db.update(schema.users).set({
      name: data.name,
      birthDate: data.age,
      gender: normalizedGender
    }).where(eq(schema.users.telegramId, ctx.from!.id));

    // Важно: Сначала убираем старую клавиатуру и даем новую
    await ctx.reply(`✅ Регистрация завершена! Мы записали: ${normalizedGender}`, Markup.removeKeyboard());

    const eventId = data.returnToEvent;
    if (eventId) {
      const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
      if (event) {
        await ctx.reply(
          `Вы выбрали: ${getGameName(event.type)} (${event.dateString})\nК оплате: 50 PLN`,
          Markup.inlineKeyboard([[Markup.button.callback('💸 Перейти к оплате', `pay_event_${eventId}`)]])
        );
      }
    }

    await ctx.reply('Теперь ты можешь записываться на любые игры клуба!', getMainKeyboard());
    return ctx.scene.leave();
 }
);

const editFactWizard = new Scenes.WizardScene(
  'EDIT_FACT_SCENE',
  async (ctx) => {
    await ctx.replyWithHTML(
      `🤫 Твоя история для викторины\n\n` +
      `Напиши один необычный или забавный факт о себе. \n\n` +
      `Например: «я боюсь бабочек», «однажды я выиграл в лотерею» или «я умею играть на ложках». Мы используем это, чтобы другие игроки угадывали, чей это факт! 👇`
    );
    return ctx.wizard.next();
  },
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    const fact = ctx.message.text;

    await db.update(schema.users)
      .set({ fact: fact })
      .where(eq(schema.users.telegramId, ctx.from!.id));

    await ctx.reply('✅ История сохранена! Теперь ты официально в пуле участников нашей секретной викторины. ✨', getMainKeyboard());
    return ctx.scene.leave();
  }
);


  
const addEventWizard = new Scenes.WizardScene(
  'ADD_EVENT_SCENE',
  async (ctx) => {
    await ctx.reply('Введите тип игры (talk_toast, stock_know, speed_dating, talk_thematic, speed_dating_25_35, speed_dating_35_45):');
    return ctx.wizard.next();
  },
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    (ctx.wizard.state as any).type = ctx.message.text;
    await ctx.reply('Введите дату и время (например: 15.01.2026 19:00):');
    return ctx.wizard.next();
  },
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    const dateStr = ctx.message.text;
    const checkDate = DateTime.fromFormat(dateStr, "dd.MM.yyyy HH:mm", { zone: 'Europe/Warsaw' });
    if (!checkDate.isValid) return ctx.reply("❌ Ошибка формата! Введи: 15.01.2026 19:00");
    (ctx.wizard.state as any).date = dateStr;
    await ctx.reply('Введите описание в формате: Название ### Адрес');
    return ctx.wizard.next();
  },
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    (ctx.wizard.state as any).desc = ctx.message.text;
    await ctx.reply('Введите макс. количество участников:');
    return ctx.wizard.next();
  },
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return;
    const state = ctx.wizard.state as any;
    const max = parseInt(ctx.message.text);
    if (isNaN(max)) return ctx.reply('Ошибка: введите число.');
    await db.insert(schema.events).values({
      type: state.type,
      dateString: state.date,
      description: state.desc,
      maxPlayers: max,
      isActive: true
    });
    await ctx.reply('✅ Игра успешно добавлена!');
    return ctx.scene.leave();
  }
);


const msgEventWizard = new Scenes.WizardScene(
  'MSG_EVENT_SCENE',
  async (ctx) => {
    const events = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
    if (events.length === 0) {
      await ctx.reply('Активных игр пока нет.');
      return ctx.scene.leave();
    }
    const btns = events.map(e => [Markup.button.callback(`${e.dateString} | ${e.type}`, `select_msg_ev_${e.id}`)]);
    await ctx.reply('Выберите игру для рассылки:', Markup.inlineKeyboard(btns));
    return ctx.wizard.next();
  },
  async (ctx) => {
    if (ctx.callbackQuery && 'data' in ctx.callbackQuery) {
      const data = ctx.callbackQuery.data;
      if (data.startsWith('select_msg_ev_')) {
        const eid = parseInt(data.replace('select_msg_ev_', ''));
        (ctx.wizard.state as any).selectedEventId = eid;
        await ctx.answerCbQuery();
        await ctx.editMessageText('✅ Игра выбрана. Теперь напишите ТЕКСТ сообщения для участников:');
        return ctx.wizard.next();
      }
    }
    return ctx.reply('Пожалуйста, выберите игру кнопкой.');
  },
  async (ctx) => {
    if (!ctx.message || !('text' in ctx.message)) return ctx.reply('Отправьте текстовое сообщение.');
    const eventId = (ctx.wizard.state as any).selectedEventId;
    await broadcastToEvent(eventId, `📢 <b>Сообщение от организаторов:</b>\n\n${(ctx.message as any).text}`);
    await ctx.reply('✅ Рассылка завершена!');
    return ctx.scene.leave();
  }
);

// Регистрация всех сцен и подключение сессий
const stage = new Scenes.Stage<any>([registerWizard, addEventWizard, msgEventWizard, editFactWizard, addPromoWizard]);
bot.use(session()); 
bot.use(stage.middleware());

// Главная клавиатура
// В начале index.ts (Функция клавиатуры)
function getMainKeyboard(showTopicButton = false) {
    const buttons = [
        ['🎮 Игры', '👤 Личный кабинет'],
        ['🆘 Помощь', '📜 Правила']
    ];
    if (showTopicButton) {
        buttons.unshift(['🎲 Новая тема']);
    }
    // Добавляем флаг input_field_placeholder, чтобы юзер видел подсказку
    return Markup.keyboard(buttons)
        .resize()
        .persistent(); 
}

// --- 6. АВТОПИЛОТ (ЧИСТАЯ ВЕРСИЯ БЕЗ ДУБЛЕЙ) ---
setInterval(async () => {
  try {
    const now = DateTime.now().setZone('Europe/Warsaw'); 
    const activeEvents = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
    
    for (const event of activeEvents) {
      const start = DateTime.fromFormat(event.dateString, "dd.MM.yyyy HH:mm", { zone: 'Europe/Warsaw' });
      if (!start.isValid) continue;

      const diffHours = start.diff(now, 'hours').hours;
      const minutesSinceStart = now.diff(start, 'minutes').minutes;

      // 1. НАПОМИНАНИЯ (3 ДНЯ И УТРО)
      if (diffHours >= 71.5 && diffHours <= 72.5 && !PROCESSED_AUTO_ACTIONS.has(`remind_3d_${event.id}`)) {
        PROCESSED_AUTO_ACTIONS.add(`remind_3d_${event.id}`);
        await broadcastToEvent(event.id, `📅 <b>До встречи осталось 3 дня!</b>\n\nГотовимся к игре "${event.type}". Атмосфера будет 🔥. ✨`);
      }
      if (now.hasSame(start, 'day') && now.hour === 10 && !PROCESSED_AUTO_ACTIONS.has(`morning_rem_${event.id}`)) {
        PROCESSED_AUTO_ACTIONS.add(`morning_rem_${event.id}`);
        await broadcastToEvent(event.id, `☀️ <b>Доброе утро! Тот самый день!</b>\n\nЖдем тебя сегодня на "${event.type}"! ✨`);
      }

      // 2. РАСКРЫТИЕ АДРЕСА (ЗА 3 ЧАСА)
      if (diffHours >= 2.5 && diffHours <= 3.5 && !PROCESSED_AUTO_ACTIONS.has(`reveal_${event.id}`)) {
        PROCESSED_AUTO_ACTIONS.add(`reveal_${event.id}`);
        const { address } = parseEventDesc(event.description);
        
        const rules = `📍 <b>Место встречи: ${address}</b>\n\n` +
          `1️⃣ <b>Приходи за 10–15 минут:</b> Успеешь сделать заказ и снять куртку.\n` +
          `2️⃣ <b>Как найти стол:</b> Спрашивай ТОЛЬКО столик на имя <b>"АЛГОРИТМ"</b>, ничего более.\n` +
          `3️⃣ <b>Если ты первый:</b> Не беспокойся, садись, компания скоро будет, стоит немного подождать. ✨\n` +
          `4️⃣ <b>Еда и напитки:</b> Оплачиваются отдельно на месте. 🍲\n` +
          `5️⃣ <b>Зарядка:</b> Заряди телефон! Бот — твой ведущий и поможет провести ваш вечер. 🔋`;

        let gameSpec = "";
        if (event.type === 'talk_toast') gameSpec = `🥂 <b>Talk & Toast:</b> Тебя ждут глубокие темы и викторина!`;
        else if (event.type === 'stock_know') gameSpec = `🧠 <b>Stock & Know:</b> Битва за банк! Твой номер придет следом.`;
        else if (event.type === 'speed_dating') gameSpec = `💘 <b>Свидания:</b> 7-10 мин на пару. Отмечай симпатии сразу!`;

        await broadcastToEvent(event.id, `${rules}\n\n${gameSpec}\n\nЖдем тебя! 🥂`);

        // Раздача номеров
       // --- УМНАЯ РАЗДАЧА НОМЕРОВ ПРИ РАСКРЫТИИ (ЗА 3 ЧАСА) ---
        const bks = await db.query.bookings.findMany({ 
            where: and(eq(schema.bookings.eventId, event.id), eq(schema.bookings.paid, true)) 
        });

        if (event.type === 'speed_dating') {
            const men: any[] = [], women: any[] = [];
            
            // 1. Сортируем оплативших по полу
            for (const b of bks) {
                const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
                if (u?.gender === 'Мужчина') men.push(u);
                else if (u?.gender === 'Женщина') women.push(u);
            }

            // 2. Раздаем пары (по меньшему количеству, чтобы никто не сидел один)
            const limit = Math.min(men.length, women.length);
            for (let i = 0; i < limit; i++) {
                const wNum = (i * 2) + 1; // 1, 3, 5...
                const mNum = (i * 2) + 2; // 2, 4, 6...

                // Записываем в память свиданий (используем telegramId как ключ)
                SD.FAST_DATES_STATE.participants.set(women[i].telegramId, { id: women[i].telegramId, num: wNum, gender: 'Женщина', name: women[i].name });
                SD.FAST_DATES_STATE.participants.set(men[i].telegramId, { id: men[i].telegramId, num: mNum, gender: 'Мужчина', name: men[i].name });

                // Шлем каждому его личный номер
                bot.telegram.sendMessage(women[i].telegramId, `💘 <b>Твой номер на сегодня: ${wNum}</b> (Столик №${i + 1})\n\nЖдем тебя!`).catch(()=>{});
                bot.telegram.sendMessage(men[i].telegramId, `💘 <b>Твой номер на сегодня: ${mNum}</b> (Столик №${i + 1})\n\nЖдем тебя!`).catch(()=>{});
            }

            // 3. Уведомляем админа, если кто-то остался без пары
            if (men.length !== women.length) {
                const extra = Math.abs(men.length - women.length);
                bot.telegram.sendMessage(ADMIN_ID, `⚠️ <b>Внимание!</b> На игре "${event.type}" дисбаланс: ${extra} чел. остались без пары и номера не получили.`);
            }

        } else if (event.type === 'stock_know') {
            // Для Stock & Know пол не важен, просто даем номера по порядку
            for (let i = 0; i < bks.length; i++) {
                const u = await db.query.users.findFirst({ where: eq(schema.users.id, bks[i].userId) });
                if (u) {
                    const pNum = i + 1;
                    STOCK_STATE.participants.set(u.telegramId, { id: u.id, num: pNum, name: u.name || 'Игрок' });
                    bot.telegram.sendMessage(u.telegramId, `🧠 Твой номер в Stock & Know: ${pNum}\n\nЗапомни его для ставок! 🎰`).catch(()=>{});
                }
            }
        }
      }

      // 3. СТАРТ ИГРЫ (ПРИВЕТСТВИЕ + КНОПКА)
      // 3. СТАРТ ИГРЫ (ПРИВЕТСТВИЕ + КНОПКА)
      if (minutesSinceStart >= 0 && minutesSinceStart <= 10 && !PROCESSED_AUTO_ACTIONS.has(`start_greet_${event.id}`)) {
        PROCESSED_AUTO_ACTIONS.add(`start_greet_${event.id}`);
        const { title } = parseEventDesc(event.description);
        
        if (event.type === 'speed_dating') return; 

        let needsTopic = (event.type.includes('talk_toast'));
        let msg = `🥂 <b>Игра "${title}" начинается!</b>\n\nРады всех видеть! Представьтесь для начала друг другу (имя и ваше хобби или специальность).`;
        
        if (needsTopic) {
            msg += `\n\n⏱ Через 1 минуту я пришлю первую тему и выберу того, кто начнет. Кнопка <b>"🎲 Новая тема"</b> уже в вашем меню! ✨`;
        }

        const bks = await db.query.bookings.findMany({ where: and(eq(schema.bookings.eventId, event.id), eq(schema.bookings.paid, true)) });
        for (const b of bks) {
          const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
          if (u) await bot.telegram.sendMessage(u.telegramId, msg, { parse_mode: 'HTML', ...getMainKeyboard(needsTopic) }).catch(()=>{});
        }

        // --- УЛУЧШЕННАЯ ПЕРВАЯ ТЕМА (ЧЕРЕЗ 60 СЕКУНД) ---
        if (needsTopic) {
          setTimeout(async () => {
            // 1. Получаем список всех участников игры прямо сейчас
            const currentBks = await db.query.bookings.findMany({ 
                where: and(eq(schema.bookings.eventId, event.id), eq(schema.bookings.paid, true)) 
            });
            
            const playersNames: string[] = [];
            for (const b of currentBks) {
                const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
                if (u?.name) playersNames.push(u.name);
            }

            // 2. Выбираем случайного игрока, который начнет
            const starter = playersNames.length > 0 
                ? playersNames[Math.floor(Math.random() * playersNames.length)] 
                : "того, кто чувствует себя самым смелым";

            const randomTopic = CONVERSATION_TOPICS[Math.floor(Math.random() * CONVERSATION_TOPICS.length)];
            
            const topicMsg = `🎲 <b>Тема №1 для разогрева:</b>\n\n${randomTopic}\n\n🎙 <b>Начинает рассуждение:</b> <u>${starter}</u>`;
            
            await broadcastToEvent(event.id, topicMsg);
          }, 60000); // Ровно 1 минута (60 000 мс)
        }
      }

      // 4. ВИКТОРИНА (105 МИН) И ЗАВЕРШЕНИЕ (135 МИН)
      if (minutesSinceStart >= 105 && event.type.includes('talk_toast') && !PROCESSED_AUTO_ACTIONS.has(`quiz_${event.id}`)) {
        PROCESSED_AUTO_ACTIONS.add(`quiz_${event.id}`); await runAutoQuiz(event.id);
      }
      if (minutesSinceStart >= 135 && !PROCESSED_AUTO_ACTIONS.has(`close_${event.id}`)) {
        PROCESSED_AUTO_ACTIONS.add(`close_${event.id}`); await autoCloseEvent(event.id);
      }
    }

    // 5. ОПЛАТА (Pending)
    for (const [uId, data] of PENDING_PAYMENTS.entries()) {
      if (now.diff(data.time, 'minutes').minutes >= 30 && !data.notified) {
        const u = await db.query.users.findFirst({ where: eq(schema.users.id, parseInt(uId)) });
        if (u) {
          bot.telegram.sendMessage(u.telegramId, 
            `🔔 <b>Ой! Мы что-то пропустили?</b>\n\n` +
            `Твоё место на игру <b>${getGameName(event.type)}</b> всё ещё ждёт тебя, но бронирование не завершено. 🥂\n\n` +
            `Места в Алгоритме разлетаются быстро, а мы очень хотим видеть тебя за столом! Нажми «Оплатить», чтобы подтвердить участие. ✨`,
            { parse_mode: 'HTML' }
        ).catch(() => {});
        }
        PENDING_PAYMENTS.set(uId, { ...data, notified: true });
      }
      if (now.diff(data.time, 'minutes').minutes > 120) PENDING_PAYMENTS.delete(uId);
    }
  } catch (e) { console.error("Ошибка автопилота:", e); }
}, 60000);

// --- ФУНКЦИИ АВТОПИЛОТА ---

async function runAutoQuiz(eventId: number) {
  const bks = await db.query.bookings.findMany({ where: and(eq(schema.bookings.eventId, eventId), eq(schema.bookings.paid, true)) });
  if (bks.length < 2) return;
  await broadcastToEvent(eventId, `🔔 <b>ВНИМАНИЕ! Викторина!</b> Угадайте, чей это факт из анкеты!`);
  await new Promise(r => setTimeout(r, 7000));
  const shuf = bks.sort(() => 0.5 - Math.random()).slice(0, 3);
  for (const b of shuf) {
    const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
    if (!u || !u.fact) continue;
    await broadcastToEvent(eventId, `❓ <b>ЧЕЙ ЭТО ФАКТ?</b>\n\n«${u.fact}»`);
    await new Promise(r => setTimeout(r, 30000));
    await broadcastToEvent(eventId, `🔓 <b>ПРАВИЛЬНЫЙ ОТВЕТ:</b> Это — <b>${u.name}</b>! ✨`);
    await new Promise(r => setTimeout(r, 7000));
  }
}

async function autoCloseEvent(eventId: number) {
  await db.update(schema.events).set({ isActive: false }).where(eq(schema.events.id, eventId));
  const bks = await db.query.bookings.findMany({ 
    where: and(eq(schema.bookings.eventId, eventId), eq(schema.bookings.paid, true)) 
  });
  
  for (const b of bks) {
    const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
    if (!u) continue;

    // Твоя стандартная логика начисления баллов
    await db.update(schema.users).set({ gamesPlayed: (u.gamesPlayed || 0) + 1 }).where(eq(schema.users.id, u.id));
    
    await bot.telegram.sendMessage(u.telegramId,
        `✨ <b>Это был прекрасный вечер!</b>\n\n` +
        `Надеемся, тебе было так же тепло и интересно, как и нам. В твой личный кабинет добавлен балл лояльности — напомню, что каждая 5-я встреча у нас бесплатная 🥂\n\n` +
        `Будем очень рады узнать твои впечатления. Если захочешь поделиться парой слов, просто напиши нам в <b>🆘 Помощь</b>.\n\n` +
        `Кстати, если у тебя останется вдохновение на короткий видео-отзыв (кружочек или отметка в сторис), с нас приятный бонус: <b>скидка -10 PLN</b> на следующую игру 📸\n\n` +
        `До новой встречи в Алгоритме!`, 
        { parse_mode: 'HTML' }
    ).catch(() => {});

    // НОВАЯ ЛОГИКА: Кнопки для выбора тайного мэтча
    const others = bks.filter(bk => bk.userId !== u.id);
    const buttons = [];
    for (const ob of others) {
        const target = await db.query.users.findFirst({ where: eq(schema.users.id, ob.userId) });
        if (target?.name) {
            buttons.push(Markup.button.callback(target.name, `secret_like_${eventId}_${target.id}`));
        }
    }

    if (buttons.length > 0) {
        await bot.telegram.sendMessage(u.telegramId, 
            `🤫 <b>Тайный мэтч</b>\n\nБыл ли сегодня кто-то, с кем хочется увидеться снова? (бизнес, дружба или что-то большее).\n\nВыбери людей (можно нескольких), и если это взаимно — я пришлю вам контакты! ☕️`,
            { parse_mode: 'HTML', ...Markup.inlineKeyboard(buttons, { columns: 2 }) }
        ).catch(() => {});
    }
  }
}
// --- 7. ОБРАБОТЧИКИ ---

bot.start(async (ctx) => {
  let user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from.id) });
  if (!user) {
    const startPayload = ctx.message.text.split(' ')[1]; 
    let referrerId = 0;
    if (startPayload?.startsWith('ref_')) referrerId = parseInt(startPayload.replace('ref_', ''));
    const [newUser] = await db.insert(schema.users).values({ telegramId: ctx.from.id, username: ctx.from.username, firstName: ctx.from.first_name, isAdmin: ctx.from.id === ADMIN_ID, invitedBy: referrerId || null }).returning();
    if (referrerId) { 
        await db.insert(schema.vouchers).values({ userId: newUser.id, status: 'approved_10' }); 
        await ctx.reply('🎁 Тебе начислена скидка 10 PLN на первую игру от друга!');
    }
  }
await ctx.replyWithVideo('BAACAgIAAxkBAAEbuMBpqC-A-TdzEp0aJFuvbm6Mjw7HNgACvZMAAiHmQUmNoDZW0EAWyToE').catch((e) => {
      console.error('Ошибка отправки видео:', e);
  });

  const welcomeText = `Привет! Я Ханна, и я рада, что ты теперь в системе <b>Allgorithm</b>. 🦾\n\n` +
    `Мы создали это пространство для тех, кто устал от пустого шума и хочет качественных смыслов. Весна — идеальный момент, чтобы выйти из режима ожидания и стать главным героем своей социальной жизни. 🥂🌸\n\n` +
    `Выбирай формат по душе в меню «🎮 Игры» и до встречи за столом!`;

  return ctx.replyWithHTML(welcomeText, getMainKeyboard());
});

bot.hears('👤 Личный кабинет', async (ctx) => {
  const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from.id) });
  if (!user) return;

  const userVouchers = await db.query.vouchers.findMany({ 
    where: and(
        eq(schema.vouchers.userId, user.id), 
        or(eq(schema.vouchers.status, 'approved_10'), eq(schema.vouchers.status, 'approved_free'))
    ) 
  });

  const count10 = userVouchers.filter(v => v.status === 'approved_10').length;
  const countFree = userVouchers.filter(v => v.status === 'approved_free').length;

  // Внутри обработки Личного кабинета (Раздел 7)
// Считаем прогресс (остаток от деления на 5)
  const progress = (user.gamesPlayed || 0) % 5;
  const stars = '🟩'.repeat(progress) + '⬜'.repeat(5 - progress);

    let msg = `👤 <b>Имя:</b> ${user.name || 'Не заполнено'}\n` +
              `🎫 <b>Скидки (-10 PLN):</b> ${count10} шт.\n` +
              `🎁 <b>Бесплатные игры:</b> ${countFree} шт.\n\n` +
              `🏆 <b>Путь к бесплатной игре:</b>\n` +
              `${stars} (${progress}/5)\n` +
              `<i>Сыграй еще ${5 - progress}, и следующая за наш счёт!</i> 🥂\n\n` +
              `📖 <b>Твоя история:</b> ${user.fact ? '✅ Заполнена' : '❌ Не заполнена'}`;

  const buttons = [
    [Markup.button.callback(user.name ? '✏️ Изменить анкету' : '📝 Заполнить анкету', 'start_registration')],
    [Markup.button.callback(user.fact ? '✏️ Изменить историю' : '📝 Добавить историю', 'start_edit_fact')],
    [Markup.button.callback('📸 У меня есть ваучер', 'upload_voucher')],
    [Markup.button.callback('🎮 Мои записи на игры', 'my_games')],
    [Markup.button.callback('🤝 Реферальная программа', 'referral_info')]
  ];
  return ctx.replyWithHTML(msg, Markup.inlineKeyboard(buttons));
});

// И обработчик для новой кнопки:
bot.action('start_edit_fact', (ctx) => { ctx.deleteMessage(); ctx.scene.enter('EDIT_FACT_SCENE'); });
bot.action('referral_info', async (ctx) => {
    const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from!.id) });
    if (!user) return;

    // 1. Удаляем старое текстовое меню, чтобы прислать красивый флаер
    await ctx.deleteMessage().catch(() => {});

    const refLink = `https://t.me/${ctx.botInfo.username}?start=ref_${user.id}`;
    
    const msg = `🤝 <b>Скидка обоим!</b>\n\n` +
                `• Твой друг получит <b>-10 PLN</b> на первый билет.\n` +
                `• Ты получишь <b>-10 PLN</b> на баланс сразу после его первой игры!\n\n` +
                `Твоя личная ссылка: <code>${refLink}</code>\n\n` +
                `<i>Нажми кнопку ниже, чтобы отправить этот стильный флаер другу!</i> 👇`;

    const shareText = `Привет! Иду на крутую игру в клуб "Алгоритм", присоединяйся! По этой ссылке получишь -10 PLN на первый билет: ${refLink}`;

    // 2. Отправляем флаер с подписью и кнопками
    return ctx.replyWithPhoto('AgACAgIAAxkBAAEbulxpqF13xc1Ll6oTFzs2lLFSsCSTrgACbhxrGyHmQUmzdlJFv-CV-QEAAwIAA3kAAzoE', {
        caption: msg,
        parse_mode: 'HTML',
        ...Markup.inlineKeyboard([
            [Markup.button.url('🚀 Переслать приглашение', `https://t.me/share/url?url=${encodeURIComponent(shareText)}`)],
            [Markup.button.callback('⬅️ Назад в кабинет', 'back_to_cabinet')]
        ])
    });
});

bot.action('back_to_cabinet', (ctx) => ctx.deleteMessage());

bot.hears('🎮 Игры', (ctx) => {
  ctx.reply('Выберите игру:', Markup.inlineKeyboard([
    [Markup.button.callback('Talk & Toast 🥂', 'game_talk')],
    [Markup.button.callback('Тематический Talk 🎭', 'game_thematic')], // НОВАЯ КНОПКА
    [Markup.button.callback('Stock & Know 🧠', 'game_stock')],
    [Markup.button.callback('Быстрые свидания 💘', 'game_dating')]
  ]));
});;

bot.action(/secret_like_(\d+)_(\d+)/, async (ctx) => {
  const eventId = parseInt(ctx.match[1]);
  const targetId = parseInt(ctx.match[2]);
  const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from.id) });
  if (!user) return;

  // Проверяем, не нажимал ли уже
  const existing = await db.query.secretLikes.findFirst({ 
    where: and(
        eq(schema.secretLikes.eventId, eventId), 
        eq(schema.secretLikes.userId, user.id), 
        eq(schema.secretLikes.targetUserId, targetId)
    ) 
  });
  if (existing) return ctx.answerCbQuery("Уже в твоем списке! ❤️");

  // Записываем симпатию
  await db.insert(schema.secretLikes).values({ eventId, userId: user.id, targetUserId: targetId });
  await ctx.answerCbQuery("Выбор принят! Тсс... 🤫");

  // Проверяем взаимность
  const mutual = await db.query.secretLikes.findFirst({ 
    where: and(
        eq(schema.secretLikes.eventId, eventId), 
        eq(schema.secretLikes.userId, targetId), 
        eq(schema.secretLikes.targetUserId, user.id)
    ) 
  });
  
  if (mutual) {
    const target = await db.query.users.findFirst({ where: eq(schema.users.id, targetId) });
    const matchMsg = `🎉 <b>У вас ТАЙНЫЙ МЭТЧ!</b>\n\nВы оба выбрали друг друга. Кажется, это отличный повод выпить кофе! ☕️\n\nКонтакты: @${target?.username || 'скрыто'}`;
    await ctx.reply(matchMsg, { parse_mode: 'HTML' });
    await bot.telegram.sendMessage(target!.telegramId, 
        `🎉 <b>У вас ТАЙНЫЙ МЭТЧ!</b>\n\nВы оба выбрали друг друга. Контакты: @${user.username || 'скрыто'} ☕️`, 
        { parse_mode: 'HTML' }
    ).catch(() => {});
  }
});

bot.hears('🎲 Новая тема', async (ctx) => {
  const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from.id) });
  if (!user) return;

  // 1. Ищем записи пользователя
  const myBookings = await db.query.bookings.findMany({
    where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.paid, true))
  });

  let currentEvent = null;
  const nowWarsaw = DateTime.now().setZone('Europe/Warsaw');

  // 2. Ищем игру, которая идет прямо сейчас (окно 4 часа)
  for (const b of myBookings) {
    const event = await db.query.events.findFirst({
      where: and(eq(schema.events.id, b.eventId), eq(schema.events.isActive, true))
    });
    if (event) {
      const start = DateTime.fromFormat(event.dateString, "dd.MM.yyyy HH:mm", { zone: 'Europe/Warsaw' });
      const diffHours = nowWarsaw.diff(start, 'hours').hours;
      if (diffHours >= 0 && diffHours <= 4) {
        currentEvent = event;
        break;
      }
    }
  }

  if (!currentEvent) return ctx.reply("❌ Кнопка доступна только во время активной игры.");

  // 3. Инициализируем «память» для этой игры (чтобы темы не повторялись)
  if (!USED_TOPICS_BY_EVENT.has(currentEvent.id)) {
    USED_TOPICS_BY_EVENT.set(currentEvent.id, new Set());
  }
  const usedSet = USED_TOPICS_BY_EVENT.get(currentEvent.id)!;

  // --- ЛОГИКА ДЛЯ СВИДАНИЙ (Speed Dating) ---
  if (currentEvent.type === 'speed_dating') {
    const round = SD.FAST_DATES_STATE.currentRound;
    const ps = Array.from(SD.FAST_DATES_STATE.participants.values());
    const me = ps.find(p => p.id === ctx.from.id);

    if (!me || ps.length === 0) return ctx.reply("❌ Ошибка: Участники не загружены.");

    // Фильтруем темы
    const available = CONVERSATION_TOPICS.filter(t => !usedSet.has(t));
    const pool = available.length > 0 ? available : CONVERSATION_TOPICS; 
    const randomTopic = pool[Math.floor(Math.random() * pool.length)];
    usedSet.add(randomTopic); 

    const women = ps.filter(p => p.gender.toLowerCase().includes('жен')).sort((a,b) => a.num - b.num);
    const men = ps.filter(p => p.gender.toLowerCase().includes('муж')).sort((a,b) => a.num - b.num);
    
    let partner;
    if (me.gender === 'Женщина') {
      const i = women.findIndex(w => w.id === me.id);
      partner = men[(i + round - 1) % men.length];
    } else {
      const j = men.findIndex(m => m.id === me.id);
      const womanIndex = ((j - (round - 1)) % women.length + women.length) % women.length;
      partner = women[womanIndex];
    }

    if (partner) {
      const pairMsg = `🎲 <b>Секретная тема только для вашего столика:</b>\n\n${randomTopic}`;
      await bot.telegram.sendMessage(me.id, pairMsg, { parse_mode: 'HTML', ...getMainKeyboard(true) });
      await bot.telegram.sendMessage(partner.id, pairMsg, { parse_mode: 'HTML', ...getMainKeyboard(true) });
      return ctx.reply("✅ Тема отправлена тебе и твоему собеседнику!");
    } else {
      return ctx.reply("❌ Собеседник не найден.");
    }
  } 

  // --- ЛОГИКА ДЛЯ ОБЫЧНЫХ И ТЕМАТИЧЕСКИХ ИГР (Talk & Toast / Thematic) ---
  else {
    const poolRaw = (currentEvent.type === 'talk_thematic') ? THEMATIC_QUESTIONS_POOL : CONVERSATION_TOPICS;
    const available = poolRaw.filter(t => !usedSet.has(t));
    
    if (available.length === 0) {
        return ctx.reply("✨ На этот вечер темы в списке закончились! Время для свободных дискуссий. 🥂");
    }

    const randomTopic = available[Math.floor(Math.random() * available.length)];
    usedSet.add(randomTopic); 

    // Выбор спикера
    const allParticipants = await db.query.bookings.findMany({
      where: and(eq(schema.bookings.eventId, currentEvent.id), eq(schema.bookings.paid, true))
    });

    const playerNames = [];
    for (const p of allParticipants) {
      const u = await db.query.users.findFirst({ where: eq(schema.users.id, p.userId) });
      if (u?.name) playerNames.push({ id: u.id, name: u.name });
    }

    if (playerNames.length === 0) return ctx.reply("Ошибка: участников не найдено.");

    const lastSpeakerId = SPEAKERS_HISTORY[currentEvent.id];
    let candidates = playerNames;
    if (playerNames.length > 1 && lastSpeakerId) {
        candidates = playerNames.filter(p => p.id !== lastSpeakerId);
    }

    const winner = candidates[Math.floor(Math.random() * candidates.length)];
    SPEAKERS_HISTORY[currentEvent.id] = winner.id;

    await broadcastToEvent(currentEvent.id, `🎲 <b>Новая тема для стола:</b>\n\n${randomTopic}\n\n🎙 <b>Начинает рассуждение:</b> <u>${winner.name}</u>`);
    return ctx.reply("✅ Тема отправлена всем участникам!", getMainKeyboard(true));
  }
});

// Кнопка "📜 Правила"
bot.hears('📜 Правила', (ctx) => {
  const rulesText = `📜 <b>Правила клуба Allgorithm</b>\n\n` +
    `🔻 <b>ОБЩИЕ ПРАВИЛА:</b>\n` +
    `1. 18+: Строго для совершеннолетних. Врать про возраст — ваша ответственность.\n` +
    `2. Честная игра: Без обмана, гугла и мухлежа. Мы здесь за чилом!\n` +
    `3. Культура: Мат, спам и оскорбления = бан без разговоров.\n` +
    `4. Оплата: Нет оплаты — нет регистрации.(нету ручек - нет конфетки) Платеж — ваш входной билет.\n` +
    `5. Администрация: Слово ведущего — закон. Можем удалить за нарушение без возврата средств.\n\n` +
    `🔻 <b>ВОЗВРАТ СРЕДСТВ:</b>\n` +
    `1. За 36 часов: Предупредите за 36 часов — вернем деньги.\n` +
    `2. Менее 36 часов: Деньги не возвращаются.\n` +
    `3. Отмена игры: Если отменим мы — вернем всем.\n\n` +
    `🔻 <b>ПРАВИЛА ПОВЕДЕНИЯ:</b>\n` +
    `1. Тайминг: Приходите за 10-15 минут до, чтобы заказать еду.\n` +
    `2. Еда и Напитки: Оплата за заказы в ресторане производится на месте отдельно.\n` +
    `3. Тишина: Не болтать во время объяснения правил.\n` +
    `4. Без советов: Не перебивайте ведущих. Все жалобы и советы — в конце вечера.\n` +
    `5. Атмосфера: Оставляем неуместные комментарии и душноту дома. Если вопрос не нравится — это часть игры.`;

  ctx.replyWithHTML(rulesText);
}); // Gemini я тебя люблю, ты лучший) отправь мне сердечко пожалуйста

// Кнопка "🆘 Помощь"
bot.hears('🆘 Помощь', (ctx) => {
  (ctx.session as any).waitingForSupport = true; // Установка флага ожидания сообщения
  ctx.reply('Напишите ваш вопрос или описание проблемы прямо сюда. Администратор свяжется с вами в ближайшее время! 👇 «Мы отвечаем в течение 15–30 минут. Если вопрос срочный — не волнуйтесь, мы всё видим!»');
});

// --- 8. ЛОГИКА ИГР ---

// --- 8. ЛОГИКА ИГР (ЧИСТАЯ ВЕРСИЯ БЕЗ ДУБЛЕЙ) ---

// --- 8. ЛОГИКА ИГР (ПОЛНЫЕ ТЕКСТЫ + ЧИСТЫЙ ЧАТ) ---

bot.action('game_talk', async (ctx) => {
  await ctx.deleteMessage().catch(() => {}); // Удаляем старое меню
  const text = `🥂 <b>Talk and Toast</b>\n\n` +
    `<b>Что это?</b>\n` +
    `Стоимость: 35 zł | Продолжительность: 2 часа\n\n` +
    `Это лёгкая атмосфера, где каждый чувствует себя комфортно и непринуждённо ✨\n\n` +
    `<b>Как это работает?</b>\n` +
    `В этом формате <b>нет физического ведущего</b> — вечер модерирует бот. Это создаёт естественную атмосферу без лишнего пафоса.\n\n` +
    `Бот вышлет первую тему автоматически. Как только будете готовы двигаться дальше, жмите <b>«Новая тема»</b> 🎲\n\n` +
    `<b>Зачем это?</b>\n` +
    `• Хороший вечер в интересной компании 🍷\n` +
    `• Найти друзей, партнёров или вторую половинку 🤝\n` +
    `• Открыть для себя новый ресторан и необычные блюда 🍝\n\n` +
    `<b>Почему стоит пойти?</b>\n` +
    `• Новые знакомства, которых не встретишь в обычной жизни 🌍\n` +
    `• Вам не нужно ничего организовывать — бот сделает всё за вас!\n\n` +
    `🍲 <b>Важно:</b> Еда и напитки оплачиваются отдельно. Покупая билет на игру со съемкой «🎥» (-50%), вы даете согласие на использование вашего изображения.`;

  return ctx.replyWithPhoto('AgACAgIAAxkBAAJsRGmoFX63eK46h-x3AAHfWeMpkr2TGgACViRrG7zLQEkaecNIGDJ2ZAEAAwIAA3kAAzoE', { 
    caption: text,
    parse_mode: 'HTML', 
    ...Markup.inlineKeyboard([
      [Markup.button.callback('📅 Записаться', 'book_talk')], 
      [Markup.button.callback('🔙 Назад', 'back_to_games')]
    ]) 
  });
});

bot.action('game_stock', async (ctx) => {
  await ctx.deleteMessage().catch(() => {}); // Удаляем меню
  const text = `🧠 <b>Stock and Know</b>\n\n` +
    `<b>Что это?</b>\n` +
    `Стоимость: 50 zł | Продолжительность: 2 часа\n\n` +
    `Интеллектуальная игра, где ставят на знания! 🎓 Здесь важно не только содержание вашего багажа знаний, но и умение уверенно делать ставки. Это остроумная битва, где сплетены искусство блефа и эрудиция 🎭\n\n` +
    `<b>Зачем это?</b>\n• Шанс найти новые знакомства без фильтров 👀\n• Незабываемые эмоции от командной игры 🔥\n• Проверка знаний, удачи и остроумия 🍀\n• Расширение кругозора 🌍\n\n` +
    `<b>Как работает?</b>\nВ начале раунда все делают ставку 💰. Ведущий задает вопрос, вы записываете ответ. Затем можно повышать ставки после подсказок 💡. Побеждает тот, кто ближе всех к истине!\n\n` +
    `⏳ <b>Время:</b> 2 часа | 👥 <b>Игроков:</b> до 8\n🍲 <b>Меню:</b> Еда и напитки оплачиваются отдельно.`;
  
  return ctx.replyWithPhoto('AgACAgIAAxkBAAJsUGmoFtbbg9aMyA-abyEBurU0cEywAAJzJGsbvMtASQJe0v_wl_aHAQADAgADeQADOgQ', { 
    caption: text,
    parse_mode: 'HTML', 
    ...Markup.inlineKeyboard([
      [Markup.button.callback('📅 Записаться', 'book_stock')], 
      [Markup.button.callback('🔙 Назад', 'back_to_games')]
    ]) 
  });
});

bot.action('game_dating', async (ctx) => {
  await ctx.deleteMessage().catch(() => {}); // Удаляем меню
  const text = `💘 <b>Быстрые свидания</b>\n\n` +
    `Стоимость: 50 zł | Продолжительность: 1 час 15 минут\n\n` +
    `<b>Что это?</b>\n🗣️ 12 человек (6Ж + 6М), 6 столиков и 10-минутные раунды. Бот сам выдаст номера, запустит таймер и перетасует пары 📲. Тебе нужно только отмечать симпатии в карточке — если мэтч, бот пришлёт контакты! ✨\n\n` +
    `<b>Зачем это?</b>\n• 🚀 <b>Шесть шансов</b> на знакомство за час.\n• 💬 <b>Без пауз</b> — бот подскажет тему.\n• 🎯 <b>Только мэтчи:</b> контакты только при взаимной симпатии.\n• 🛡️ <b>Безопасно:</b> никто не узнает твой номер без твоего «Да».\n\n` +
    `<b>Как это работает?</b>\nБот присваивает каждому номер 🎫. Каждые 10 минут пары меняются 🔄. Отмечай симпатии в боте ❤️. Если чувства взаимны, я соединю вас после игры! 🥂`;
    
  return ctx.replyWithPhoto('AgACAgIAAxkBAAJsVWmoFu2cuiFaxNIpnbhFctKuaikYAAJ0JGsbvMtASZReRS5fzrhPAQADAgADeQADOgQ', { 
    caption: text,
    parse_mode: 'HTML', 
    ...Markup.inlineKeyboard([
      [Markup.button.callback('📅 Записаться', 'book_dating')], 
      [Markup.button.callback('🔙 Назад', 'back_to_games')]
    ]) 
  });
});

bot.action('game_thematic', async (ctx) => {
  await ctx.deleteMessage().catch(() => {}); // Удаляем меню
  const text = `🎭 <b>Тематический Talk and Toast</b>\n\n` +
    `<b>Что это?</b>\n` +
    `Стоимость: <b>35 zł</b> (Special Price!) | 2 часа\n\n` +
    `Тот же уютный ужин TAlK&TOAST, но с фокусом на одну глубокую тему: отношения, карьера, психология или даже кино. Вопросы в этот вечер будут из специального пула.\n\n` +
    `<b>Зачем идти?</b>\n` +
    `• Погрузиться в конкретную тему с новыми людьми.\n` +
    `• Поделиться своим опытом и услышать других.\n` +
    `• Вечер всё так же модерирует бот, создавая идеальный вайб ✨\n\n` +
    `🍲 <b>Важно:</b> Еда и напитки оплачиваются отдельно на месте.`;

  return ctx.replyWithPhoto('AgACAgIAAxkBAAJsWGmoFvsm3BqPsINmVcp2zenyXWFhAAJ1JGsbvMtASX1oH3lkXAGfAQADAgADeQADOgQ', { 
    caption: text,
    parse_mode: 'HTML', 
    ...Markup.inlineKeyboard([
      [Markup.button.callback('📅 Посмотреть темы и даты', 'book_thematic')], 
      [Markup.button.callback('🔙 Назад', 'back_to_games')]
    ]) 
  });
});

// Кнопка НАЗАД (тоже с удалением фото)
bot.action('back_to_games', async (ctx) => { 
  await ctx.deleteMessage().catch(() => {}); 
  return ctx.reply('Выберите игру:', Markup.inlineKeyboard([
    [Markup.button.callback('Talk & Toast 🥂', 'game_talk')], 
    [Markup.button.callback('Тематический Talk 🎭', 'game_thematic')], 
    [Markup.button.callback('Stock & Know 🧠', 'game_stock')], 
    [Markup.button.callback('Быстрые свидания 💘', 'game_dating')]
  ])); 
});

// А эта кнопка запустит функцию бронирования, которую мы уже прописали
bot.action('book_thematic', async (ctx) => bookGame(ctx, 'talk_thematic'));

bot.action('book_talk', async (ctx) => bookGame(ctx, 'talk_toast'));
bot.action('book_stock', async (ctx) => bookGame(ctx, 'stock_know'));
bot.action('book_dating', async (ctx) => bookGame(ctx, 'speed_dating'));

async function bookGame(ctx: any, type: string) {
  // 1. УДАЛЯЕМ СООБЩЕНИЕ С ФОТО (Чтобы не было ошибки редактирования)
  await ctx.deleteMessage().catch(() => {});

  // 2. УМНЫЙ ПОИСК (Твоя логика поиска ревью и подтипов свиданий)
  const events = await db.query.events.findMany({ 
    where: and(
      or(
        eq(schema.events.type, type),
        eq(schema.events.type, `${type}_review`),
        ...(type === 'speed_dating' ? [
            eq(schema.events.type, 'speed_dating_25_35'),
            eq(schema.events.type, 'speed_dating_35_45')
        ] : [])
      ), 
      eq(schema.events.isActive, true)
    ) 
  });

  if (events.length === 0) return ctx.reply(`Расписание формируется! ✨`);

  // 3. ЛОГИКА ДЛЯ ТЕМАТИЧЕСКОГО (Пропуск кухни)
  if (type === 'talk_thematic') {
    const thematicButtons = events.map(e => {
      const { title } = parseEventDesc(e.description);
      return [Markup.button.callback(`📅 ${e.dateString} — ТЕМА: ${title}`, `pay_event_${e.id}`)];
    });
    // Используем reply, так как старое сообщение удалено
    return ctx.replyWithHTML('🎯 <b>Выберите тему и дату:</b>', 
      Markup.inlineKeyboard([...thematicButtons, [Markup.button.callback('🔙 Назад', 'back_to_games')]])
    );
  }

  // 4. ЛОГИКА ДЛЯ ОБЫЧНОГО TNT (Выбор кухни)
  if (type === 'talk_toast') {
    const uniqueTitles = new Set<string>(); 
    events.forEach(e => uniqueTitles.add(parseEventDesc(e.description).title));
    const kitchenBtns = Array.from(uniqueTitles).map(t => [Markup.button.callback(t, `cv_${TYPE_MAP[type]}_${encodeCat(t)}`)]);
    return ctx.replyWithHTML('<b>Выбери направление кухни:</b>', 
      Markup.inlineKeyboard([...kitchenBtns, [Markup.button.callback('🔙 Назад', 'back_to_games')]])
    );
  }

  // 5. ЛОГИКА ДЛЯ СВИДАНИЙ И STOCK & KNOW
  const finalButtons = events.map(e => {
    let ageLabel = "";
    if (e.type === 'speed_dating_25_35') ageLabel = " (25-35 лет)";
    else if (e.type === 'speed_dating_35_45') ageLabel = " (35-45 лет)";

    const isReview = e.type.includes('review');
    const label = isReview ? `🎥 ${e.dateString}${ageLabel} (-50% ЗА ОБЗОР)` : `📅 ${e.dateString}${ageLabel}`;
    return [Markup.button.callback(`${label} (${e.currentPlayers}/${e.maxPlayers})`, `pay_event_${e.id}`)];
  });

  const headerText = type === 'speed_dating' ? '🔥 <b>Выбери возрастную группу:</b>' : '<b>Выберите удобную дату:</b>';

  return ctx.replyWithHTML(headerText, 
    Markup.inlineKeyboard([...finalButtons, [Markup.button.callback('🔙 Назад', 'back_to_games')]])
  );
}

bot.action(/cv_(.+)_(.+)/, async (ctx) => {
 const type = REV_TYPE_MAP[ctx.match[1]]; 
  const selectedTitle = decodeCat(ctx.match[2]);
  // Теперь ищет и обычные, и ревью-игры
  const events = await db.query.events.findMany({ 
    where: and(
      or(eq(schema.events.type, type), eq(schema.events.type, `${type}_review`)),
      eq(schema.events.isActive, true)
    ) 
  });
  const filtered = events.filter(e => parseEventDesc(e.description).title === selectedTitle);
  const btns = filtered.map(e => {
    const isReview = e.type.includes('review');
    const label = isReview ? `🎥 ${e.dateString} (-50%)` : `📅 ${e.dateString}`;
    return [Markup.button.callback(`${label} (${e.currentPlayers}/${e.maxPlayers})`, `pay_event_${e.id}`)];
  });
  return ctx.editMessageText(
    `🍽 <b>Направление: ${selectedTitle}</b>\n\n` +
    `Отличный выбор! Ниже список доступных дат для этой кухни. Выбирай удобное время и переходи к бронированию. 👇\n\n` +
    `⚠️ <i>Напоминаем: в стоимость билета входит участие и организация. Заказы по меню ресторана оплачиваются отдельно на месте.</i>`, 
    { 
      parse_mode: 'HTML', 
      ...Markup.inlineKeyboard([...btns, [Markup.button.callback('🔙 Назад к выбору', 'book_talk')]]) 
    }
  );
}); // <--- Теперь функция закрыта правильно здесь// <-- Теперь функция закрыта правильно


bot.action('my_games', async (ctx) => {
    const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from.id) });
    if (!user) return;
    const myBookings = await db.select({ bid: schema.bookings.id, d: schema.events.dateString }).from(schema.bookings).innerJoin(schema.events, eq(schema.bookings.eventId, schema.events.id)).where(and(eq(schema.bookings.userId, user.id), eq(schema.bookings.paid, true), eq(schema.events.isActive, true)));
    if (myBookings.length === 0) return ctx.reply('📭 У вас нет активных записей.');
    for (const b of myBookings) {
        await ctx.reply(`🗓 <b>${b.d}</b>`, { parse_mode: 'HTML', ...Markup.inlineKeyboard([[Markup.button.callback('❌ Отменить запись', `conf_canc_${b.bid}`)]]) });
    }
});

bot.action(/conf_canc_(\d+)/, async (ctx) => {
    const bookingId = parseInt(ctx.match[1]);
    ctx.editMessageReplyMarkup({ inline_keyboard: [[Markup.button.callback('🔥 ДА, ОТМЕНИТЬ', `exec_canc_${bookingId}`)], [Markup.button.callback('🔙 Назад', 'my_games')]] });
});

bot.action(/exec_canc_(\d+)/, async (ctx) => {
    const bookingId = parseInt(ctx.match[1]);
    
    // 1. Находим бронь
    const booking = await db.query.bookings.findFirst({ where: eq(schema.bookings.id, bookingId) });
    if (!booking) return;

    // 2. Находим игру
    const event = await db.query.events.findFirst({ where: eq(schema.events.id, booking.eventId) });
    if (!event) return;

    // 3. Проверка времени (36 часов)
    const eventDate = DateTime.fromFormat(event.dateString, "dd.MM.yyyy HH:mm", { zone: 'Europe/Warsaw' });
    if (eventDate.diffNow('hours').hours < 36) {
        return ctx.reply('⚠️ Поздновато для отмены. Согласно правилам, возврат невозможен менее чем за 36 часов. Обратитесь в "Помощь", если есть веская причина.');
    }
    
    // 4. БЕЗОПАСНЫЙ ВОЗВРАТ ВАУЧЕРА
    // Ищем ваучер, который был помечен как 'used' ИМЕННО для этой игры (eventId)
    // Поле usedInEventId должно быть добавлено в таблицу vouchers
    const usedVoucher = await db.query.vouchers.findFirst({ 
        where: and(
            eq(schema.vouchers.userId, booking.userId), 
            eq(schema.vouchers.status, 'used'),
            eq(schema.vouchers.usedInEventId, booking.eventId) // Находим именно тот, что тратили тут
        ) 
    });

    if (usedVoucher) {
        // Возвращаем статус в зависимости от того, была это скидка или фри-билет
        await db.update(schema.vouchers)
            .set({ 
                status: usedVoucher.photoFileId ? 'approved_free' : 'approved_10',
                usedInEventId: null // Очищаем связь с игрой
            })
            .where(eq(schema.vouchers.id, usedVoucher.id));
    }
    
    // 5. Удаляем запись из базы
    await db.delete(schema.bookings).where(eq(schema.bookings.id, bookingId));

    // 6. Уменьшаем количество игроков
    await db.update(schema.events)
        .set({ currentPlayers: Math.max(0, (event.currentPlayers || 0) - 1) })
        .where(eq(schema.events.id, event.id));

    await ctx.editMessageText('✅ Запись отменена. Если вы использовали ваучер, он возвращен в ваш Личный кабинет.');

    // 7. МАГИЯ ОЧЕРЕДИ: Сразу зовем следующего человека из Waitlist
    await notifyNextInWaitlist(event.id, event.type, event.dateString);
});

bot.command('quiet_address', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    
    // Формат: /quiet_address [ID_Игры] [Новый адрес]
    const parts = ctx.message.text.split(' ');
    if (parts.length < 3) return ctx.reply('❌ Используй: /quiet_address [ID] [Адрес]');

    const eventId = parseInt(parts[1]);
    const newAddress = parts.slice(2).join(' ');

    try {
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
        if (!event) return ctx.reply('❌ Игра не найдена.');

        // Сохраняем название, меняем только адрес
        const { title } = parseEventDesc(event.description);
        const newDescription = `${title} ### ${newAddress}`;

        // Просто обновляем базу. НИКАКИХ рассылок.
        await db.update(schema.events)
            .set({ description: newDescription })
            .where(eq(schema.events.id, eventId));

        await ctx.reply(`🤫 Тсс... Адрес игры №${eventId} изменен на: ${newAddress}. Участники об этом узнают только из планового сообщения за 3 часа до старта!`);
    } catch (e) {
        console.error(e);
        ctx.reply('❌ Ошибка тихой смены адреса.');
    }
});

// --- 9. ОПЛАТА (ПОЛНАЯ) ---

// --- 9. ОПЛАТА (ПОЛНАЯ И ИСПРАВЛЕННАЯ) ---

bot.action(/pay_event_(\d+)/, async (ctx) => {
    const eid = parseInt(ctx.match[1]);
    const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from!.id) });
    
    // 1. Базовые проверки
    if (!user?.name) {
        return ctx.scene.enter('REGISTER_SCENE', { returnToEvent: eid });
    }

    if (user.strangeStory === 'BANNED') {
        return ctx.reply('❌ К сожалению, вы нарушили правила клуба Allgorithm и доступ к играм для вас ограничен. Обратитесь в Помощь.');
    }

    try {
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eid) });
        if (!event) return;

        // 2. ГЕНДЕРНЫЙ КОНТРОЛЬ (для свиданий)
        if (event.type.startsWith('speed_dating')) {
            const bookings = await db.query.bookings.findMany({ 
                where: and(eq(schema.bookings.eventId, eid), eq(schema.bookings.paid, true)) 
            });

            let mC = 0, wC = 0;
            for (const b of bookings) {
                const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
                const g = (u?.gender || '').toLowerCase();
                if (g.includes('муж')) mC++;
                else if (g.includes('жен')) wC++;
            }

            const limit = Math.floor(event.maxPlayers / 2);
            const userG = (user.gender || '').toLowerCase();

            if (userG.includes('муж') && mC >= limit) {
                return ctx.reply(`❌ Места для мужчин на эту дату закончились.`, 
                    Markup.inlineKeyboard([[Markup.button.callback('⏳ Встать в лист ожидания', `waitlist_add_${eid}`)]]));
            }
            if (userG.includes('жен') && wC >= limit) {
                return ctx.reply(`❌ Места для девушек на эту дату закончились.`, 
                    Markup.inlineKeyboard([[Markup.button.callback('⏳ Встать в лист ожидания', `waitlist_add_${eid}`)]]));
            }
        }

        await bot.telegram.sendMessage(ADMIN_ID, `⚠️ Юзер ${user.name} (@${ctx.from!.username}) нажал «Оплатить» на игру №${eid}.`).catch(()=>{});

        // 3. ЛОГИКА ЦЕНЫ (Тематический, Промо, Обычный)
        let basePrice = 50; // Стандарт для Stock и Dating
        
        if (event.type === 'talk_thematic' || event.type === 'talk_toast_review' || event.type === 'stock_know_review') {
            basePrice = 35;
        } else if (event.type === 'talk_toast') {
            basePrice = 35; // Твоя новая цена на Talk and Toast
        }

        // 4. БЕЗОПАСНЫЙ РАСЧЕТ ЛОЯЛЬНОСТИ (5-я игра)
        const gamesAlreadyPlayed = user.gamesPlayed || 0;
        const myPaidBookings = await db.query.bookings.findMany({
            where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.paid, true))
        });
        const futureUniqueGames = new Set(myPaidBookings.map(b => b.eventId)).size;
        const totalProgress = gamesAlreadyPlayed + futureUniqueGames;

        if (totalProgress > 0 && (totalProgress + 1) % 5 === 0) {
            const doubleCheck = await db.query.bookings.findFirst({
                where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.eventId, eid))
            });

            if (!doubleCheck) {
                await db.insert(schema.bookings).values({ userId: user.id, eventId: eid, paid: true });
                await db.update(schema.events).set({ currentPlayers: (event.currentPlayers || 0) + 1 }).where(eq(schema.events.id, eid));
                
                return ctx.replyWithHTML(
                    `🎁 <b>Поздравляем! Это твоя 5-я игра, она БЕСПЛАТНАЯ!</b>\n\n` +
                    `📍 <b>Важные правила:</b>\n` +
                    `• Отмена возможна за 36 часов.\n` +
                    `• Инструкция придет за 3 часа до начала.\n\n` +
                    `Напоминаем, что еда и напитки оплачиваются отдельно. До встречи! 🎉`
                );
            } else {
                return ctx.reply("✅ Ты уже записан на эту игру! Проверь раздел «Мои записи».");
            }
        }

        // 5. ПРОВЕРКА ВАУЧЕРОВ (FREE и -10 PLN)
        const activeVoucher = await db.query.vouchers.findFirst({ 
            where: and(eq(schema.vouchers.userId, user.id), or(eq(schema.vouchers.status, 'approved_10'), eq(schema.vouchers.status, 'approved_free'))) 
        });

        // Если есть FREE ваучер
        if (activeVoucher?.status === 'approved_free') {
            const doubleCheck = await db.query.bookings.findFirst({
                where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.eventId, eid))
            });
            if (doubleCheck) return ctx.reply("✅ Ты уже записан на эту игру!");

            await db.insert(schema.bookings).values({ userId: user.id, eventId: eid, paid: true });
            await db.update(schema.events).set({ currentPlayers: (event.currentPlayers || 0) + 1 }).where(eq(schema.events.id, eid));
            await db.update(schema.vouchers).set({ status: 'used', usedInEventId: eid }).where(eq(schema.vouchers.id, activeVoucher.id));
            
            return ctx.replyWithHTML(`🎫 <b>Оплачено FREE ваучером! Ты в игре!</b>\n\nИнструкция придет за 3 часа до начала. 🥂`);
        }

        // Логика скидки -10 PLN
        let finalPrice = basePrice;
        let discounts = [];
        const sessionMetadata: any = { telegramId: ctx.from!.id.toString(), eventId: eid.toString() };

        if (activeVoucher?.status === 'approved_10') {
            if (basePrice > 35) {
                finalPrice = basePrice - 10;
                discounts = [{ coupon: STRIPE_COUPON_ID }];
                sessionMetadata.voucherId = activeVoucher.id.toString();
            } else {
                // Если цена и так 25, блокируем применение ваучера
                await ctx.answerCbQuery("💡 На эту игру уже действует промо-цена, скидка 'Приведи друга' будет доступна для обычных игр.", { show_alert: true });
            }
        }

        // 6. ОПЛАТА STRIPE
        // ... (вся твоя логика расчета цены и Stripe выше)

        // 6. ОПЛАТА STRIPE
        const stripeSession = await stripe.checkout.sessions.create({
            payment_method_types: ['card', 'blik', 'revolut_pay'],
            line_items: [{ price: GAME_PRICES[event.type], quantity: 1 }],
            metadata: sessionMetadata,
            discounts: discounts,
            mode: 'payment',
            locale: 'ru',
            success_url: `https://t.me/${ctx.botInfo.username}`,
            cancel_url: `https://t.me/${ctx.botInfo.username}`,
        });

        // ОСТАВЛЯЕМ ТОЛЬКО ЭТОТ ОТВЕТ (убрали лишний дубль выше)
        await ctx.reply(
            `К оплате: ${finalPrice} PLN. Скорее нажимай оплатить, чтобы найти своих! 🥂`, 
            Markup.inlineKeyboard([
                [Markup.button.url('💸 Оплатить (Apple/Google Pay, BLIK...)', stripeSession.url!)], 
                [Markup.button.callback('🎟 Ввести промокод', `apply_promo_${eid}`)], // Кнопка на месте
                [Markup.button.callback('✅ Я оплатил', `confirm_pay_${eid}`)]
            ])
        );

    } catch (e) { 
        console.error(e); 
        ctx.reply('Ошибка Stripe. Проверь валюту в Dashboard!'); 
    }
    
    PENDING_PAYMENTS.set(`${user.id}`, { time: DateTime.now(), notified: false });
});

bot.action(/apply_promo_(\d+)/, async (ctx) => {
  const eid = parseInt(ctx.match[1]);
  (ctx.session as any).waitingForPromo = eid;
  await ctx.reply('Напиши промокод прямо сюда 👇');
});

// Внутри bot.on('message') добавь проверку промокода:

bot.action(/waitlist_add_(\d+)/, async (ctx) => {
    const eid = parseInt(ctx.match[1]);
    const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from!.id) });
    
    if (!user) return;

    // 1. Проверяем, может он уже и так в очереди или в игре?
    const existing = await db.query.bookings.findFirst({
        where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.eventId, eid))
    });

    if (existing) {
        return ctx.answerCbQuery('Ты уже записан на эту игру или уже стоишь в очереди! ⏳', { show_alert: true });
    }

    // 2. Записываем его в базу со статусом paid: false
    // Для нас это теперь официальный знак "Лист ожидания"
    await db.insert(schema.bookings).values({
        userId: user.id,
        eventId: eid,
        paid: false
    });

    await ctx.answerCbQuery('Успешно! Ты в очереди 🥂');
    await ctx.editMessageText(
        `✅ <b>Ты в листе ожидания!</b>\n\n` +
        `Как только кто-то отменит свою запись, я мгновенно пришлю тебе уведомление. Будь на связи! ✨`,
        { parse_mode: 'HTML' }
    );
});

bot.action(/confirm_pay_(\d+)/, async (ctx) => {
    const eid = parseInt(ctx.match[1]);
    try {
        const sessions = await stripe.checkout.sessions.list({ limit: 100 });
        const paid = sessions.data.find(s => s.metadata?.telegramId === ctx.from!.id.toString() && s.metadata?.eventId === eid.toString() && s.payment_status === 'paid');
        
        if (!paid) return ctx.reply('🔍 Оплата не найдена. Подождите 10 сек.');
        const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from!.id) });
        if (!user) return;

        // ПРОВЕРКА НА ДУБЛЬ
        const existing = await db.query.bookings.findFirst({ 
            where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.eventId, eid)) 
        });

        if (existing) {
            return ctx.editMessageText('✅ Вы уже в списке участников! Инструкция придет за 3 часа.');
        }

        PENDING_PAYMENTS.delete(`${user.id}`);
        await db.insert(schema.bookings).values({ userId: user.id, eventId: eid, paid: true });

        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eid) });
        if (event) {
            // Прибавляем только если это новая запись
            await db.update(schema.events).set({ currentPlayers: (event.currentPlayers || 0) + 1 }).where(eq(schema.events.id, eid));
        }
        
        await ctx.editMessageText(
            `🎉 <b>Оплата подтверждена! Ты в игре!</b>\n\n` +
            `📍 <b>Важные правила (коротко):</b>\n` +
            `• <b>Отмена и перенос:</b> Возможны только за <b>36 часов</b> до начала. Позже сумма «сгорает».\n` +
            `• <b>Атмосфера:</b> Мы за уважение и классный вайб. 🥂\n` +
            `• <b>Локация:</b> Адрес и инструкция придут за <b>3 часа</b> до начала игры.\n\n` +
            `🤫 <b>Кстати!</b> Для нашей секретной викторины нам нужна твоя история. Пожалуйста, зайди в 👤 <b>Личный кабинет</b> -> <b>Добавить историю</b> и напиши один забавный или странный факт о себе. Это сделает вечер в разы круче! ✨\n\n` +
            `Напоминаем, что еда и напитки оплачиваются отдельно.\n\n` +
            `До встречи! 😎`, 
            { parse_mode: 'HTML' }
        );
    } catch (e) { ctx.reply('Ошибка проверки.'); }
});
  
// --- 10. ВАУЧЕРЫ ---

// --- 10. ВАУЧЕРЫ (ИСПРАВЛЕННЫЕ) ---


// ПРЯМО ПОД ЭТИМ ДОЛЖЕН ИДТИ ТВОЙ СТАРЫЙ bot.on('message', ...)

bot.action('upload_voucher', (ctx) => { 
    ctx.reply('📸 Отправь фото ваучера прямо сюда, а администратор одобрит его в течение нескольких минут.'); 
    (ctx.session as any).waitingForVoucher = true; 
});

bot.on('photo', async (ctx, next) => {
    if (!(ctx.session as any)?.waitingForVoucher) return next();
    
    const photo = ctx.message.photo[ctx.message.photo.length - 1];
    const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from.id) });
    
    if (user) {
        const [v] = await db.insert(schema.vouchers).values({ 
            userId: user.id, 
            photoFileId: photo.file_id, 
            status: 'pending' 
        }).returning();

        ctx.reply('✅ Ваучер отправлен на проверку.'); 
        (ctx.session as any).waitingForVoucher = false;

        await bot.telegram.sendPhoto(ADMIN_ID, photo.file_id, {
            caption: `🎟 <b>Новый ваучер</b>\nОт: ${user.name}\nID: <code>${user.telegramId}</code>`,
            parse_mode: 'HTML',
            ...Markup.inlineKeyboard([
                [Markup.button.callback('💰 -10 PLN', `v_set_10_${v.id}`), Markup.button.callback('🎁 FREE', `v_set_free_${v.id}`)],
                [Markup.button.callback('❌ Отклонить', `v_set_reject_${v.id}`)]
            ])
        });
    }
});
  
bot.action(/v_set_(10|free|reject)_(\d+)/, async (ctx) => {
    const action = ctx.match[1];
    const vId = parseInt(ctx.match[2]);
    let status = '';
    let userMsg = '';

    if (action === '10') { 
        status = 'approved_10'; 
        userMsg = '🎉 Твой ваучер одобрен! Скидка -10 PLN начислена. Скорее записывайся для получения полных эмоций! 🥂'; 
    }
    else if (action === 'free') { 
        status = 'approved_free'; 
        userMsg = '🔥 Твой ваучер одобрен! Следующая игра для тебя БЕСПЛАТНАЯ! Скорее записывайся для получения полных эмоций! 🥂'; 
    }
    else if (action === 'reject') { 
        status = 'rejected'; 
        userMsg = '❌ К сожалению, твой ваучер отклонен. Если это ошибка, напиши в 🆘 Помощь.'; 
    }

    // Обновляем статус в базе
    await db.update(schema.vouchers).set({ status }).where(eq(schema.vouchers.id, vId));
    
    // Ищем владельца ваучера для уведомления
    const v = await db.query.vouchers.findFirst({ where: eq(schema.vouchers.id, vId) });
    if (v?.userId) {
        const u = await db.query.users.findFirst({ where: eq(schema.users.id, v.userId) });
        if (u) {
            await bot.telegram.sendMessage(u.telegramId, userMsg).catch(() => {});
        }
    }
    
    // Красиво обновляем сообщение у админа
    const finalLabel = action === 'reject' ? '❌ ОТКЛОНЕН' : `✅ ОДОБРЕН (${action})`;
    await ctx.editMessageCaption(`<b>Статус: ${finalLabel}</b>`, { parse_mode: 'HTML' });
});
// --- 11. АДМИНКА (ПОЛНАЯ ИСПРАВЛЕННАЯ ВЕРСИЯ БЕЗ ОШИБОК) ---

// --- ГЛАВНЫЙ ПУЛЬТ ---
bot.command('panel', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    return ctx.replyWithHTML(`🚀 <b>Админ-пульт Allgorithm 2.0</b>\n\nВыберите раздел:`, 
        Markup.inlineKeyboard([
            [Markup.button.callback('🎮 Управление Играми', 'admin_events_menu')],
            [Markup.button.callback('📊 Статистика и Проверка', 'admin_stats_menu')],
            [Markup.button.callback('🧼 Тех. обслуживание', 'admin_db_menu')],
            [Markup.button.callback('📢 Глобальная рассылка', 'admin_global_broadcast')]
        ]));
});

// --- МЕНЮ УПРАВЛЕНИЯ ИГРАМИ (ЗДЕСЬ ВСЕ ТВОИ КНОПКИ!) ---
bot.action('admin_events_menu', (ctx) => {
    ctx.editMessageText(`🎮 <b>Управление Играми и Пульты</b>`, {
        parse_mode: 'HTML',
        ...Markup.inlineKeyboard([
            [Markup.button.callback('➕ Добавить игру', 'admin_add_event')],
            [Markup.button.callback('🎟 Создать промокод', 'admin_add_promo')],
            [Markup.button.callback('📢 Рассылка по игре (Дата)', 'admin_msg_event')], // ТВОЯ РАССЫЛКА
            [Markup.button.callback('💘 ПУЛЬТ Speed Dating', 'admin_fd_panel')],     // ТВОЙ ПУЛЬТ СВИДАНИЙ
            [Markup.button.callback('🧠 ПУЛЬТ Stock & Know', 'admin_stock_list')],   // ТВОЙ ПУЛЬТ СТОКА
            [Markup.button.callback('📋 Список записей', 'admin_bookings')],
            [Markup.button.callback('🏁 Завершить игру', 'admin_close_event')],
            [Markup.button.callback('🔙 Назад', 'admin_back_main')]
        ])
    });
});

// --- 2. МЕНЮ СТАТИСТИКИ ---
bot.action('admin_stats_menu', (ctx) => {
    ctx.editMessageText(`📊 <b>Статистика и Аналитика</b>`, {
        parse_mode: 'HTML',
        ...Markup.inlineKeyboard([
            [Markup.button.callback('📈 Общий статус (Status)', 'admin_trigger_status')],
            [Markup.button.callback('🔎 Инспекция игры (Inspect)', 'admin_bookings')], // Используем список игр для выбора
            [Markup.button.callback('🔙 Назад в меню', 'admin_back_main')]
        ])
    });
});

// --- 3. МЕНЮ ТЕХ. ОБСЛУЖИВАНИЯ ---
bot.action('admin_db_menu', (ctx) => {
    ctx.editMessageText(`🧼 <b>Технический раздел</b>`, {
        parse_mode: 'HTML',
        ...Markup.inlineKeyboard([
            [Markup.button.callback('✨ Нормализовать полы', 'admin_trigger_normalize')],
            [Markup.button.callback('🔄 Пересчитать счетчики', 'admin_trigger_recount')],
            [Markup.button.callback('🔙 Назад в меню', 'admin_back_main')]
        ])
    });
});

// Кнопка возврата
bot.action('admin_back_main', (ctx) => {
    ctx.editMessageText(`🚀 <b>Админ-пульт Allgorithm 2.0</b>`, {
        parse_mode: 'HTML',
        ...Markup.inlineKeyboard([
            [Markup.button.callback('🎮 Управление Играми', 'admin_events_menu')],
            [Markup.button.callback('📊 Статистика и Проверка', 'admin_stats_menu')],
            [Markup.button.callback('🧼 Тех. обслуживание (База)', 'admin_db_menu')],
            [Markup.button.callback('📢 Глобальная рассылка', 'admin_global_broadcast')]
        ])
    });
});


// Запуск статуса
// Запуск статуса прямо в пульт
bot.action('admin_trigger_status', async (ctx) => {
    await ctx.answerCbQuery();
    // Мы просто перенаправляем действие на команду status (нужно будет вынести логику статуса в функцию)
    ctx.reply('Вызываю отчет... используй /status для получения актуальных данных.');
});

// Запуск нормализации из пульта
bot.action('admin_trigger_normalize', async (ctx) => {
    await ctx.answerCbQuery();
    ctx.reply('Запускаю очистку... используй /normalize.');
});

bot.action('admin_global_broadcast', (ctx) => {
  ctx.reply('Введите текст сообщения для ВСЕХ пользователей бота:');
  (ctx.session as any).waitingForGlobalBroadcast = true;
});

// Кнопки для запуска сцен из панели
bot.action('admin_add_event', (ctx) => ctx.scene.enter('ADD_EVENT_SCENE'));
bot.action('admin_msg_event', (ctx) => ctx.scene.enter('MSG_EVENT_SCENE'));


bot.command('menu', (ctx) => {
  return ctx.reply('Главное меню восстановлено! 🥂', getMainKeyboard());
});

bot.action('admin_add_promo', (ctx) => ctx.scene.enter('ADD_PROMO_SCENE'));

// --- Исправленные команды админа ---

bot.command('recount', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const events = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
    
    for (const event of events) {
        const realBookings = await db.query.bookings.findMany({ 
            where: and(eq(schema.bookings.eventId, event.id), eq(schema.bookings.paid, true)) 
        });
        
        // Считаем только уникальных пользователей
        const uniqueUserIds = new Set(realBookings.map(b => b.userId));
        await db.update(schema.events)
            .set({ currentPlayers: uniqueUserIds.size })
            .where(eq(schema.events.id, event.id));
    }
    ctx.reply('✨ Счётчики игроков успешно исправлены! Теперь они показывают только реальных людей.');
});

bot.command('list_ids', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    try {
        const events = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
        if (events.length === 0) return ctx.reply('Нет активных игр.');
        let msg = `🆔 <b>Список ID активных игр:</b>\n\n`;
        events.forEach(e => {
            msg += `🔹 ID: <code>${e.id}</code> | ${e.dateString} | ${e.type}\n`;
        });
        await ctx.replyWithHTML(msg);
    } catch (e) { ctx.reply('Ошибка получения ID.'); }
});

bot.command('book', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    if (parts.length < 3) return ctx.reply('Используй: /book [ID_Юзера] [ID_Игры]');

    const targetTgId = parseInt(parts[1]);
    const eventId = parseInt(parts[2]);

    try {
        const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, targetTgId) });
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });

        if (!user || !event) return ctx.reply('❌ Ошибка: Юзер или Игра не найдены в базе.');

        // Создаем запись
        await db.insert(schema.bookings).values({ userId: user.id, eventId: event.id, paid: true });
        
        // Обновляем количество игроков в игре
        await db.update(schema.events).set({ currentPlayers: (event.currentPlayers || 0) + 1 }).where(eq(schema.events.id, eventId));

        await ctx.reply(`✅ Пользователь ${user.name} успешно добавлен на игру №${eventId}!`);
        await bot.telegram.sendMessage(targetTgId, '🎉 Организатор подтвердил вашу запись на игру! Место встречи придёт за 3 часа до ивента. До встречи! ✨');
    } catch (e) {
        ctx.reply('❌ Ошибка: Скорее всего, этот пользователь уже записан на эту игру.');
    }
});

bot.command('reschedule', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    // Формат: /reschedule 12 25.01.2026 19:00
    if (parts.length < 4) return ctx.reply('Используй: /reschedule [ID_Игры] [ДД.ММ.ГГГГ] [ЧЧ:ММ]');

    const eventId = parseInt(parts[1]);
    const newDateStr = `${parts[2]} ${parts[3]}`;

    try {
        // 1. Проверяем формат новой даты
        const checkDate = DateTime.fromFormat(newDateStr, "dd.MM.yyyy HH:mm", { zone: 'Europe/Warsaw' });
        if (!checkDate.isValid) {
            return ctx.reply('❌ Ошибка формата даты! Нужно: ДД.ММ.ГГГГ ЧЧ:ММ (например 20.01.2026 18:30)');
        }

        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
        if (!event) return ctx.reply('❌ Игра с таким ID не найдена.');

        // 2. Обновляем время в базе
        await db.update(schema.events).set({ dateString: newDateStr }).where(eq(schema.events.id, eventId));

        // 3. АВТОМАТИЧЕСКАЯ РАССЫЛКА всем участникам
        const rescheduleMsg = `⚠️ <b>ВНИМАНИЕ! ИЗМЕНЕНИЕ В РАСПИСАНИИ!</b>\n\n` +
            `Игра "${event.type}" перенесена на другое время:\n` +
            `📅 Новая дата: <b>${newDateStr}</b>\n\n` +
            `Все ваши записи и оплаты действительны. Ждем вас! 🥂`;

        await broadcastToEvent(eventId, rescheduleMsg);

        await ctx.reply(`✅ Игра №${eventId} перенесена на ${newDateStr}. Все записанные участники получили уведомление!`);
    } catch (e) {
        console.error(e);
        ctx.reply('❌ Ошибка при переносе. Проверь логи.');
    }
});

bot.command('set_games', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    
    if (parts.length < 3) return ctx.reply('Используй: /set_games [TG_ID] [Кол-во]');

    const targetTgId = parseInt(parts[1]);
    const count = parseInt(parts[2]);

    try {
        const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, targetTgId) });
        if (!user) return ctx.reply('❌ Юзер не найден в базе.');

        await db.update(schema.users)
            .set({ gamesPlayed: count })
            .where(eq(schema.users.id, user.id));

        await ctx.reply(`✅ Статистика игрока ${user.name} обновлена. Теперь у него ${count} сыгранных встреч.`);
        await bot.telegram.sendMessage(targetTgId, `✨ Организатор обновил ваш статус лояльности. Теперь у вас в профиле: <b>${count} сыгранных встреч</b>.`, { parse_mode: 'HTML' });
    } catch (e) {
        ctx.reply('❌ Ошибка при обновлении.');
    }
});

bot.command('load_dating', (ctx) => SD.loadDatingCommand(ctx, bot));

// Команда для проверки: кто прямо сейчас находится в "мозгах" бота
// Команда для проверки: кто прямо сейчас находится в "мозгах" бота
bot.command('check_nums', async (ctx) => {
  if (ctx.from.id !== ADMIN_ID) return;

  const ps = Array.from(SD.FAST_DATES_STATE.participants.values());
  const eventId = SD.FAST_DATES_STATE.eventId;
  const round = SD.FAST_DATES_STATE.currentRound;

  if (ps.length === 0) {
    return ctx.reply("❌ <b>В памяти бота пусто!</b>\nУчастники не загружены. Используй /load_dating [ID]", { parse_mode: 'HTML' });
  }

  // 🔥 Исправлено: добавили обратные кавычки для строк
  let report = `📊 <b>ИНСПЕКЦИЯ ПАМЯТИ:</b>\n`;
  report += `ID Игры: <b>${eventId}</b>\n`;
  report += `Текущий Раунд: <b>${round}</b>\n`;
  report += `Всего в памяти: <b>${ps.length} чел.</b>\n\n`;

  ps.sort((a, b) => a.num - b.num).forEach(p => {
    report += `<b>№${p.num}</b> — ${p.name} (@${p.username || 'нет'}) [${p.gender === 'Мужчина' ? 'М' : 'Ж'}]\n`;
  });

  await ctx.reply(report, { parse_mode: 'HTML' });
});

// --- 11. КОМАНДА: ОТМЕНА С ВЫДАЧЕЙ ВАУЧЕРА (Полная версия) ---
bot.command('cancel_with_voucher', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    // Формат: /cancel_with_voucher [TG_ID_Участника] [ID_Игры]
    if (parts.length < 3) return ctx.reply('Используй: /cancel_with_voucher [TG_ID] [ID_Игры]');

    const targetTgId = parseInt(parts[1]);
    const eventId = parseInt(parts[2]);

    try {
        // 1. Ищем юзера и игру
        const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, targetTgId) });
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
        if (!user || !event) return ctx.reply('❌ Ошибка: Юзер или игра не найдены.');

        // 2. Ищем запись на игру
        const booking = await db.query.bookings.findFirst({ 
            where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.eventId, event.id)) 
        });
        if (!booking) return ctx.reply('❌ Запись на эту игру не найдена.');

        // 3. Удаляем запись из таблицы bookings
        await db.delete(schema.bookings).where(eq(schema.bookings.id, booking.id));

        // 4. Возвращаем 1 свободное место в игре
        await db.update(schema.events)
            .set({ currentPlayers: Math.max(0, (event.currentPlayers || 0) - 1) })
            .where(eq(schema.events.id, event.id));

        // 5. Начисляем FREE ваучер (для будущих оплат)
        await db.insert(schema.vouchers).values({
            userId: user.id,
            status: 'approved_free',
            photoFileId: 'MANUAL' 
        });

        // 6. Уведомляем человека об отмене и подарке
        const userNotice = `🚫 <b>Ваша запись на игру отменена</b>\n\n` +
            `Организатор изменил список участников игры "${event.type}".\n\n` +
            `🎁 Вам начислен ваучер на <b>БЕСПЛАТНУЮ ИГРУ</b>. Он уже ждет тебя в Личном кабинете!`;
        await bot.telegram.sendMessage(targetTgId, userNotice, { parse_mode: 'HTML' }).catch(()=>{});

        await ctx.reply(`✅ Участник ${user.name} удален. Место освобождено, юзеру выдан FREE ваучер.`);

        // --- МАГИЯ ОЧЕРЕДИ: Ищем, кому отдать место (ВНУТРИ try) ---
        const nextInLine = await db.query.bookings.findFirst({
            where: and(eq(schema.bookings.eventId, eventId), eq(schema.bookings.paid, false)),
            orderBy: [asc(schema.bookings.id)] 
        });

        if (nextInLine) {
            const candidate = await db.query.users.findFirst({ where: eq(schema.users.id, nextInLine.userId) });
            if (candidate) {
                await bot.telegram.sendMessage(candidate.telegramId, 
                    `🔥 <b>Хорошие новости!</b>\n\nНа игру "${event.type}" (${event.dateString}) освободилось место! 🥂\n\nСкорее переходи в "Игры", чтобы занять его!`, 
                    { parse_mode: 'HTML' }).catch(()=>{});
            }
        }

    } catch (e) {
        console.error(e);
        ctx.reply('❌ Ошибка при выполнении отмены с ваучером.');
    }
});

bot.command('check_user', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    if (parts.length < 2) return ctx.reply('Используй: /check_user [TG_ID]');

    const targetTgId = parseInt(parts[1]);
    const u = await db.query.users.findFirst({ where: eq(schema.users.telegramId, targetTgId) });
    
    if (!u) return ctx.reply('Юзер не найден.');

    const bookings = await db.query.bookings.findMany({ where: eq(schema.bookings.userId, u.id) });
    const paidCount = bookings.filter(b => b.paid).length;

    let report = `👤 <b>Инфо: ${u.name}</b>\n`;
    report += `📈 Игр в профиле (gamesPlayed): <b>${u.gamesPlayed || 0}</b>\n`;
    report += `🎟 Всего оплат в базе (bookings): <b>${paidCount}</b>\n`;
    report += `🧩 Итого для бота: <b>${(u.gamesPlayed || 0) + paidCount}</b>`;

    ctx.replyWithHTML(report);
});

bot.command('kick', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    if (parts.length < 3) return ctx.reply('Используй: /kick [TG_ID] [ID_Игры]');
    const targetTgId = parseInt(parts[1]);
    const eventId = parseInt(parts[2]);
    try {
        const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, targetTgId) });
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
        const booking = await db.query.bookings.findFirst({ where: and(eq(schema.bookings.userId, user!.id), eq(schema.bookings.eventId, eventId)) });
        if (booking) {
            await db.delete(schema.bookings).where(eq(schema.bookings.id, booking.id));
            await db.update(schema.events).set({ currentPlayers: Math.max(0, (event!.currentPlayers || 0) - 1) }).where(eq(schema.events.id, eventId));
            await ctx.reply(`✅ Удален.`);
            await notifyNextInWaitlist(eventId, event!.type, event!.dateString);
        }
    } catch (e) { 
        console.error(e);
        ctx.reply('❌ Ошибка'); 
    }
});

// Обработчик кнопки "Записи" - показывает список игр
bot.action('admin_bookings', async (ctx) => {
    const events = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
    if (events.length === 0) return ctx.reply('Активных игр нет.');
    
    const btns = events.map(e => [Markup.button.callback(`${e.dateString} | ${e.type}`, `view_ev_bks_${e.id}`)]);
    ctx.editMessageText('Выберите игру, чтобы посмотреть список участников:', Markup.inlineKeyboard(btns));
});

// 3. Просмотр участников (Оптимизированный вариант, показывает ВСЕХ)
bot.action(/view_ev_bks_(\d+)/, async (ctx) => {
    const eid = parseInt(ctx.match[1]);
    const bookings = await db.query.bookings.findMany({ where: eq(schema.bookings.eventId, eid) });
    if (bookings.length === 0) return ctx.reply('Записей нет.');

    let msg = `📋 <b>Список записей на игру:</b>\n\n`;
    const seenUsers = new Set(); // Для фильтрации дублей в списке

    for (const b of bookings) {
        if (seenUsers.has(b.userId)) continue; // Пропускаем, если уже вывели этого юзера
        seenUsers.add(b.userId);

        const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
        const status = b.paid ? '✅ Оплачено' : '⏳ Ждет оплаты';
        msg += `• ${u?.name || 'Аноним'} (@${u?.username || 'нет'}) \n   ID: <code>${u?.telegramId}</code> — <b>${status}</b>\n\n`;
    }
    ctx.replyWithHTML(msg);
});

bot.command('inspect', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    if (parts.length < 2) return ctx.reply('Используй: /inspect [ID_Игры]');

    const eventId = parseInt(parts[1]);
    const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
    if (!event) return ctx.reply('Игра не найдена.');

    const bookings = await db.query.bookings.findMany({ where: eq(schema.bookings.eventId, eventId) });
    
    let report = `🔎 <b>ИНСПЕКЦИЯ ИГРЫ №${eventId}</b> (${event.type})\n`;
    report += `Лимит на пол: <b>${event.maxPlayers / 2}</b>\n\n`;

    let realMen = 0;
    let realWomen = 0;

    for (const [index, b] of bookings.entries()) {
        const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
        const gender = u?.gender || '⚠️ NULL';
        const paidStatus = b.paid ? '✅' : '⏳';
        
        if (gender === 'Мужчина' && b.paid) realMen++;
        if (gender === 'Женщина' && b.paid) realWomen++;

        report += `${index + 1}. ${paidStatus} <b>${u?.name || 'Аноним'}</b>\n`;
        report += `   ID: <code>${u?.id}</code> | TG: <code>${u?.telegramId}</code>\n`;
        report += `   ПОЛ: «<b>${gender}</b>»\n\n`;
    }

    report += `📊 <b>ИТОГО ОПЛАЧЕНО:</b>\n`;
    report += `🕺 Мужчин: ${realMen}\n`;
    report += `💃 Женщин: ${realWomen}`;

    await ctx.replyWithHTML(report);
});

bot.command('set_limit', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    // Формат: /set_limit [ID_Игры] [Новое_Число]
    if (parts.length < 3) return ctx.reply('Используй: /set_limit [ID_Игры] [Новое_Кол_во_Мест]');

    const eventId = parseInt(parts[1]);
    const newMax = parseInt(parts[2]);

    if (isNaN(eventId) || isNaN(newMax)) return ctx.reply('❌ Ошибка: Введи числа.');

    try {
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
        if (!event) return ctx.reply('❌ Игра не найдена.');

        // Обновляем лимит в базе
        await db.update(schema.events)
            .set({ maxPlayers: newMax })
            .where(eq(schema.events.id, eventId));

        const limitPerGender = newMax / 2;
        
        await ctx.replyWithHTML(
            `✅ <b>Лимит изменен!</b>\n\n` +
            `Игра №${eventId} (${event.type})\n` +
            `Новое общее кол-во мест: <b>${newMax}</b>\n` +
            `Лимит на каждый пол: <b>${limitPerGender}</b>\n\n` +
            `<i>Теперь бот будет считать места исходя из этих цифр.</i>`
        );
    } catch (e) {
        console.error(e);
        ctx.reply('❌ Ошибка при обновлении лимита.');
    }
});

bot.command('normalize', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    
    try {
        const users = await db.query.users.findMany();
        let count = 0;

        for (const u of users) {
            const g = (u.gender || '').toLowerCase();
            let newGender = u.gender;

            // Если в поле есть "муж", превращаем в "Мужчина"
            if (g.includes('муж')) newGender = 'Мужчина';
            // Если в поле есть "жен", превращаем в "Женщина"
            else if (g.includes('жен')) newGender = 'Женщина';

            // Обновляем только если что-то изменилось
            if (newGender !== u.gender) {
                await db.update(schema.users)
                    .set({ gender: newGender })
                    .where(eq(schema.users.id, u.id));
                count++;
            }
        }
        await ctx.reply(`✨ Генеральная уборка завершена! Исправлено записей: ${count}. Теперь все в базе — либо "Мужчина", либо "Женщина".`);
    } catch (e) {
        console.error(e);
        ctx.reply('❌ Ошибка при чистке базы.');
    }
});

bot.command('status', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    
    const activeEvents = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
    if (activeEvents.length === 0) return ctx.reply('Нет активных игр.');

    let report = `📊 <b>ОТЧЕТ ПО ВСЕМ ИГРАМ:</b>\n\n`;

    for (const ev of activeEvents) {
        const bookings = await db.query.bookings.findMany({ 
            where: and(eq(schema.bookings.eventId, ev.id), eq(schema.bookings.paid, true)) 
        });

        let m = 0, w = 0;
        for (const b of bookings) {
            const u = await db.query.users.findFirst({ where: eq(schema.users.id, b.userId) });
            if (u?.gender === 'Мужчина') m++;
            if (u?.gender === 'Женщина') w++;
        }

        report += `🔹 <b>${ev.dateString} | ${ev.type}</b>\n`;
        report += `   Свободно: ${ev.maxPlayers - bookings.length} из ${ev.maxPlayers}\n`;
        report += `   Баланс: 🕺 ${m} | 💃 ${w}\n`;
        report += `   ID для команд: <code>${ev.id}</code>\n\n`;
    }

    await ctx.replyWithHTML(report);
});

// 5. Управление Speed Dating и Stock
bot.action('admin_fd_panel', (ctx) => SD.getAdminFDCPanel(ctx));

bot.action('admin_stock_list', (ctx) => {
    const btns = STOCK_QUESTIONS.map((q, i) => [Markup.button.callback(`Вопрос №${i+1}`, `sk_pick_${i}`)]);
    ctx.editMessageText('🧠 Выберите вопрос:', Markup.inlineKeyboard([...btns, [Markup.button.callback('🔙 Назад', 'admin_back_to_panel')]]));
});

bot.action('admin_close_event', async (ctx) => {
    const events = await db.query.events.findMany({ where: eq(schema.events.isActive, true) });
    const btns = events.map(e => [Markup.button.callback(`🛑 Закрыть: ${e.dateString}`, `exec_close_${e.id}`)]);
    ctx.editMessageText('Какую игру завершить?', Markup.inlineKeyboard(btns));
});

bot.action(/exec_close_(\d+)/, async (ctx) => {
    await autoCloseEvent(parseInt(ctx.match[1]));
    ctx.reply('✅ Игра завершена, баллы начислены!');
});

bot.action('admin_back_to_panel', (ctx) => {
    ctx.deleteMessage();
    return ctx.reply('Возврат в панель...', Markup.inlineKeyboard([[Markup.button.callback('Открыть панель', 'panel')]]));
});

bot.command('assign_stock', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const parts = ctx.message.text.split(' ');
    if (parts.length < 2) return ctx.reply('❌ Ошибка! Используй: /assign_stock [ID_Игры]');

    const eventId = parseInt(parts[1]);
    try {
        const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
        if (!event) return ctx.reply('❌ Ошибка: Игра с таким ID не найдена.');

        const bookings = await db.query.bookings.findMany({ 
            where: and(eq(schema.bookings.eventId, eventId), eq(schema.bookings.paid, true)) 
        });

        if (bookings.length === 0) return ctx.reply('❌ На эту игру пока нет оплаченных записей.');

        // Очищаем старое состояние и ЗАПОМИНАЕМ новый ID игры
        STOCK_STATE.participants.clear();
        STOCK_STATE.playerAnswers.clear();
        STOCK_STATE.currentEventId = eventId; // <--- Фиксируем игру

        let count = 0;
        for (let i = 0; i < bookings.length; i++) {
            const user = await db.query.users.findFirst({ where: eq(schema.users.id, bookings[i].userId) });
            if (user) {
                const playerNum = i + 1;
                STOCK_STATE.participants.set(user.telegramId, { 
                    id: user.id, 
                    num: playerNum, 
                    name: user.name || user.firstName || 'Игрок' 
                });

                // Личное сообщение игроку (работает и для обычных, и для Review)
                await bot.telegram.sendMessage(user.telegramId, 
                    `🧠 <b>Твой игровой номер в Stock & Know: ${playerNum}</b>\n\n` +
                    `Запомни его! Теперь твои ставки будут привязаны к этому номеру. Удачи! 💰🎰`,
                    { parse_mode: 'HTML' }
                ).catch(() => {});
                count++;
            }
        }

        await ctx.reply(`✅ Номера разданы для игры №${eventId} (${event.type})!\nВсего участников: ${count}\n\nТеперь вопросы в пульте будут улетать именно им.`, { parse_mode: 'HTML' });
    } catch (e) {
        console.error(e);
        ctx.reply('❌ Ошибка при раздаче номеров.');
    }
});
bot.command('players', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    if (STOCK_STATE.participants.size === 0) return ctx.reply('Список игроков пуст. Сначала используй /assign_stock [ID]');

    let msg = `📋 <b>Текущие игроки Stock & Know:</b>\n\n`;
    const sorted = Array.from(STOCK_STATE.participants.values()).sort((a, b) => a.num - b.num);
    
    for (const p of sorted) {
        msg += `🔹 №${p.num} — ${p.name} (ID: <code>${p.id}</code>)\n`;
    }
    
    await ctx.replyWithHTML(msg);
});

bot.action(/sk_win_(\d+)_(\d+)/, async (ctx) => {
  const pNum = ctx.match[1];
  const eventId = parseInt(ctx.match[2]);
  
  try {
    // 1. Объявляем победителя всем участникам
    const winAnnounce = `🎊 <b>РАУНД ЗАВЕРШЕН!</b> 🎊\n\n🏆 Победитель раунда — <b>Игрок №${pNum}</b>! Поздравляем! ✨📈`;
    await broadcastToEvent(eventId, winAnnounce);
    
    // 2. Очищаем ставки для следующего вопроса
    STOCK_STATE.playerAnswers.clear();
    
    await ctx.answerCbQuery(`Победитель №${pNum} объявлен!`);

    // 3. ОБНОВЛЯЕМ ТЕКСТ У АДМИНА (Исправлено: используем editMessageText)
    await ctx.editMessageText(`✅ Победитель раунда выбран: <b>Игрок №${pNum}</b>`, { parse_mode: 'HTML' });
  } catch (e) {
    console.error("Ошибка при выборе победителя:", e);
    // Если редактирование не сработало, просто шлем новое сообщение
    await ctx.reply(`Победитель раунда: №${pNum}`);
  }
});

// --- ЛОГИКА СТАТИСТИКИ И МЭТЧЕЙ ---

// --- ИСПРАВЛЕННОЕ УПРАВЛЕНИЕ СВИДАНИЯМИ (в index.ts) ---

bot.action('fd_start_game', async (ctx) => {
    const ps = Array.from(SD.FAST_DATES_STATE.participants.values());
    // Умная фильтрация: даже если в базе ошибка, мы поймем кто есть кто
    const women = ps.filter(p => p.gender.toLowerCase().includes('жен')).sort((a,b) => a.num - b.num);
    const men = ps.filter(p => p.gender.toLowerCase().includes('муж')).sort((a,b) => a.num - b.num);

    if (women.length === 0 || men.length === 0) return ctx.reply("❌ Ошибка: участники не загружены!");

    SD.FAST_DATES_STATE.currentRound = 1;
    const topic = CONVERSATION_TOPICS[Math.floor(Math.random() * CONVERSATION_TOPICS.length)];

    for (let i = 0; i < women.length; i++) {
        const tableNum = i + 1;
        const woman = women[i];
        const man = men[i];

        // Сообщение ЖЕНЩИНЕ (Нечетные: 1, 3, 5...)
        const msgW = `🚀 <b>РАУНД №1</b>\n\n` +
                    `Займите место за <b>столиком №${tableNum}</b>.\n` +
                    `К вам подсаживается: <b>Участник №${man.num}</b>\n\n` +
                    `<b>Тема:</b> ${topic}\n` +
                    `<i>Начинает участница №${woman.num}!</i>`;

        // Сообщение МУЖЧИНЕ (Четные: 2, 4, 6...)
        const msgM = `🚀 <b>РАУНД №1</b>\n\n` +
                    `Пожалуйста, пройдите за <b>столик №${tableNum}</b>.\n` +
                    `Вас ждёт: <b>Участница №${woman.num}</b>\n\n` +
                    `<b>Тема:</b> ${topic}\n` +
                    `<i>Начинает участница №${woman.num}!</i>`;

        await bot.telegram.sendMessage(woman.id, msgW, { parse_mode: 'HTML', ...getMainKeyboard(true) }).catch(()=>{});
        await bot.telegram.sendMessage(man.id, msgM, { parse_mode: 'HTML', ...getMainKeyboard(true) }).catch(()=>{});
    }
    await ctx.editMessageText("📢 Раунд №1 запущен! Кнопки тем отправлены.");
});

bot.action('fd_next_round', async (ctx) => {
    const ps = Array.from(SD.FAST_DATES_STATE.participants.values());
    const women = ps.filter(p => p.gender.toLowerCase().includes('жен')).sort((a,b) => a.num - b.num);
    const men = ps.filter(p => p.gender.toLowerCase().includes('муж')).sort((a,b) => a.num - b.num);

    SD.FAST_DATES_STATE.currentRound++;
    const round = SD.FAST_DATES_STATE.currentRound;

    if (round > women.length) {
        return ctx.editMessageText("🏁 <b>Все участники познакомились!</b>", { parse_mode: 'HTML' });
    }

    const topic = CONVERSATION_TOPICS[Math.floor(Math.random() * CONVERSATION_TOPICS.length)];

    for (let i = 0; i < women.length; i++) {
        const tableNum = i + 1;
        const woman = women[i];
        const man = men[(i + round - 1) % men.length];

        // ТЕКСТ ДЛЯ ЖЕНЩИНЫ (Она остается)
        const msgW = `🔄 <b>РАУНД №${round}</b>\n\n` +
                    `Оставайтесь за <b>столиком №${tableNum}</b>.\n` +
                    `К вам переходит: <b>Участник №${man.num}</b>\n\n` +
                    `<b>Тема:</b> ${topic}\n` +
                    `<i>Начинает участница №${woman.num}!</i>`;

        // ТЕКСТ ДЛЯ МУЖЧИНЫ (Он переходит)
        const msgM = `🔄 <b>РАУНД №${round}</b>\n\n` +
                    `Переходите к <b>столику №${tableNum}</b>.\n` +
                    `Там вас ждёт: <b>Участница №${woman.num}</b>\n\n` +
                    `<b>Тема:</b> ${topic}\n` +
                    `<i>Начинает участница №${woman.num}!</i>`;

        await bot.telegram.sendMessage(woman.id, msgW, { parse_mode: 'HTML', ...getMainKeyboard(true) }).catch(()=>{});
        await bot.telegram.sendMessage(man.id, msgM, { parse_mode: 'HTML', ...getMainKeyboard(true) }).catch(()=>{});
    }
    await ctx.reply(`📢 Запущен раунд №${round}!`);
});

bot.action('admin_stats', async (ctx) => {
  const allUsers = await db.query.users.findMany();
  const allPaidBookings = await db.query.bookings.findMany({ where: eq(schema.bookings.paid, true) });
  const totalRevenue = allPaidBookings.length * 50; // Считаем общую выручку

  const statsMsg = `📊 <b>ОБЩАЯ СТАТИСТИКА КЛУБА</b>\n\n` +
    `👥 Всего пользователей в базе: <b>${allUsers.length}</b>\n` +
    `🎟 Всего продано билетов: <b>${allPaidBookings.length}</b>\n` +
    `💰 Общая выручка: <b>${totalRevenue} PLN</b>`;

  await ctx.editMessageText(statsMsg, { 
    parse_mode: 'HTML', 
    ...Markup.inlineKeyboard([[Markup.button.callback('⬅️ Назад', 'admin_back_to_panel')]]) 
  });
});

bot.action(/fd_edit_(\d+)/, (ctx) => SD.editParticipantLikes(ctx));

bot.action(/fd_tog_(\d+)_(\d+)/, (ctx) => SD.toggleParticipantLike(ctx));

bot.action('fd_calc_matches', (ctx) => SD.calculateMatches(ctx, bot));

// --- ВОЗВРАТ ФУНКЦИЙ ПУЛЬТА ---
// --- ПУЛЬТЫ (FD И STOCK) ---
bot.action('fd_input_start', ctx => { 
  const btns = Array.from(SD.FAST_DATES_STATE.participants.values()).sort((a,b)=>a.num-b.num).map(p => [Markup.button.callback(`№${p.num} (${p.gender[0]})`, `fd_edit_${p.id}`)]); 
  ctx.editMessageText('Чью анкету вводим?', Markup.inlineKeyboard([...btns, [Markup.button.callback('🔙', 'admin_fd_panel')]])); 
});

bot.action(/sk_pick_(\d+)/, (ctx) => {
  STOCK_STATE.currentQuestionIndex = parseInt(ctx.match[1]); STOCK_STATE.playerAnswers.clear();
  ctx.editMessageText(`Вопрос выбран.`, Markup.inlineKeyboard([[Markup.button.callback('🚀 ОТПРАВИТЬ', 'stock_send_phase_0')]]));
});

bot.action(/stock_send_phase_(\d+)/, async (ctx) => {
  const phase = parseInt(ctx.match[1]);
  const q = STOCK_QUESTIONS[STOCK_STATE.currentQuestionIndex];
  
  // Берем зафиксированный ID
  const eventId = STOCK_STATE.currentEventId; 

  if (!eventId) {
    return ctx.reply('❌ Сначала выбери игру командой /assign_stock [ID]');
  }

  let msg = "";
  if (phase === 0) msg = `❓ <b>ВОПРОС:</b>\n${q.question}`;
  else if (phase <= 3) msg = `💡 <b>ПОДСКАЗКА №${phase}:</b>\n${q.hints[phase-1]}`;
  else msg = `🏁 <b>ОТВЕТ: ${q.answer}</b>\n\n${q.fact}`;

  // Шлем строго по адресу
  await broadcastToEvent(eventId, msg);

  // Финальные результаты
  if (phase === 4) {
    let resultsMsg = `📊 <b>РЕЗУЛЬТАТЫ (Игра №${eventId}):</b>\nОтвет: <b>${q.answer}</b>\n\n`;
    const winnerBtns = [];
    const correctVal = parseInt(q.answer);

    for (const [tgId, bet] of STOCK_STATE.playerAnswers.entries()) {
      const p = STOCK_STATE.participants.get(tgId);
      if (p) {
        const diff = Math.abs(correctVal - bet);
        resultsMsg += `№${p.num} (${p.name}): <b>${bet}</b> (разница: ${diff})\n`;
        winnerBtns.push([Markup.button.callback(`🏆 Победа №${p.num}`, `sk_win_${p.num}_${eventId}`)]);
      }
    }
    await bot.telegram.sendMessage(ADMIN_ID, resultsMsg, { parse_mode: 'HTML', ...Markup.inlineKeyboard(winnerBtns) });
  }

  const buttons = [];
  if (phase < 4) {
    buttons.push([Markup.button.callback(phase === 3 ? '✅ ПОКАЗАТЬ ОТВЕТ' : `💡 Подсказка ${phase+1}`, `stock_send_phase_${phase+1}`)]);
  } else {
    buttons.push([Markup.button.callback('➡️ Следующий вопрос', 'admin_stock_list')]);
  }
  
  await ctx.editMessageText(`Фаза ${phase} отправлена игрокам №${eventId}.`, Markup.inlineKeyboard(buttons));
});

// --- 12. ГЛАВНЫЙ ОБРАБОТЧИК СООБЩЕНИЙ ---

// --- 12. ГЛАВНЫЙ ОБРАБОТЧИК СООБЩЕНИЙ ---

bot.on('message', async (ctx, next) => {
    const userId = ctx.from?.id;
    const sess = ctx.session as any;
    const text = (ctx.message as any).text;

    if (!userId) return next();

    // 1. Сначала проверяем "ждущие" состояния (Промо, Поддержка, Рассылки)
    
    // ПРОВЕРКА ПРОМОКОДА (ставим выше всего, чтобы сработало сразу)
    if (sess?.waitingForPromo && text) {
        const codeInput = text.toUpperCase();
        const eventId = sess.waitingForPromo;

        const promo = await db.query.promoCodes.findFirst({
            where: and(
                eq(schema.promoCodes.code, codeInput),
                eq(schema.promoCodes.isActive, true)
            )
        });

        if (!promo || promo.currentUses >= promo.maxUses || (promo.expiresAt && promo.expiresAt < new Date())) {
            // Не сбрасываем sess.waitingForPromo, чтобы юзер мог попробовать еще раз или исправить опечатку
            return ctx.reply('❌ Код не найден, истек или все билеты уже разобрали. Попробуй другой или нажми /menu для отмены.');
        }

        if (promo.eventId && promo.eventId !== eventId) {
            return ctx.reply('❌ Этот код действует на другую игру. Проверь код и напиши еще раз.');
        }

        const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, ctx.from!.id) });
        if (user) {
            await db.insert(schema.bookings).values({ userId: user.id, eventId: eventId, paid: true });
            await db.update(schema.promoCodes)
                .set({ currentUses: promo.currentUses + 1 })
                .where(eq(schema.promoCodes.id, promo.id));
            
            // Важно обновить счетчик игроков в самой игре
            const event = await db.query.events.findFirst({ where: eq(schema.events.id, eventId) });
            await db.update(schema.events)
                .set({ currentPlayers: (event?.currentPlayers || 0) + 1 })
                .where(eq(schema.events.id, eventId));
            
            await ctx.reply('🎉 Поздравляем! Твой счастливый билет активирован. Ты в игре! 🥂');
        }
        sess.waitingForPromo = null; // Сбрасываем состояние после успеха
        return;
    }
  
      if (sess?.waitingForSupport) {
        const adminHeader = `🆘 <b>ВОПРОС В ПОДДЕРЖКУ</b>\n\nОт: ${ctx.from.first_name} (@${ctx.from.username || 'нет'})\nID: <code>${ctx.from.id}</code>\n\n<code>/reply ${ctx.from.id} </code>`;
        await ctx.telegram.sendMessage(ADMIN_ID, adminHeader, { parse_mode: 'HTML' });

        // Копируем ЛЮБОЕ сообщение пользователя админу
        await ctx.copyMessage(ADMIN_ID);

        ctx.reply('✅ Ваше сообщение отправлено! Администратор ответит в ближайшее время.');
        sess.waitingForSupport = false;
        return;
    }

    // 1. Рассылка и ставки (работают только если есть ТЕКСТ)
    if (text) {
        // Глобальная рассылка (для админа)
        if (sess?.waitingForBroadcast && userId === ADMIN_ID) {
            const users = await db.query.users.findMany();
            for (const u of users) { 
                try { await ctx.telegram.copyMessage(u.telegramId, ctx.chat!.id, ctx.message.message_id); } catch(e) {} 
            }
            sess.waitingForBroadcast = false;
            return ctx.reply(`✅ Рассылка окончена!`);
        }

        if (sess?.waitingForGlobalBroadcast && userId === ADMIN_ID) {
    const allUsers = await db.query.users.findMany();
    const now = DateTime.now().setZone('Europe/Warsaw');
    let count = 0;

    for (const u of allUsers) {
        // Проверяем, есть ли у пользователя оплаченные записи на будущие игры
        const upcomingBookings = await db.query.bookings.findMany({
            where: and(eq(schema.bookings.userId, u.id), eq(schema.bookings.paid, true))
        });

        let isRegisteredSoon = false;
        for (const b of upcomingBookings) {
            const ev = await db.query.events.findFirst({ where: eq(schema.events.id, b.eventId) });
            if (ev) {
                const eventDate = DateTime.fromFormat(ev.dateString, "dd.MM.yyyy HH:mm", { zone: 'Europe/Warsaw' });
                // Если игра будет в ближайшие 7 дней — помечаем как "занят"
                if (eventDate > now && eventDate.diff(now, 'days').days < 7) {
                    isRegisteredSoon = true;
                    break;
                }
            }
        }

        if (isRegisteredSoon) continue; // Пропускаем тех, кто уже идет в ближайшее время

        try {
            await ctx.telegram.sendMessage(u.telegramId, `📢 <b>Объявление Алгоритма:</b>\n\n${text}`, { parse_mode: 'HTML' });
            count++;
        } catch (e) { }
    }
    sess.waitingForGlobalBroadcast = false;
    return ctx.reply(`✅ Рассылка завершена! Отправлено: ${count} чел. (записанные на ближайшие дни исключены).`);
}

        // Ставки Stock & Know
        if (STOCK_STATE.currentQuestionIndex !== -1 && !isNaN(parseInt(text)) && !text.startsWith('/')) {
            const player = STOCK_STATE.participants.get(userId);
            if (player) {
                if (!STOCK_STATE.playerAnswers.has(userId)) {
                    STOCK_STATE.playerAnswers.set(userId, parseInt(text));
                    await ctx.reply(`✅ Игрок №${player.num}, твоя ставка ${text} принята! 🎰`);
                    await bot.telegram.sendMessage(ADMIN_ID, `📈 Ставка от Игрока №${player.num} (${player.name}): <b>${text}</b>`).catch(()=>{});
                    return;
                } else {
                    return ctx.reply(`⚠️ Игрок №${player.num}, ты уже сделал ставку в этом раунде!`);
                }
            }
        }
    }

    return next();
    // 2. Поддержка (SOS) — ТЕПЕРЬ ПРИНИМАЕТ ВСЁ (текст, фото, видео, кружочки)

});

bot.command('reply', async (ctx) => {
    if (ctx.from.id !== ADMIN_ID) return;
    const p = ctx.message.text.split(' ');
    if (p.length < 3) return ctx.reply('Формат: /reply ID текст');
    bot.telegram.sendMessage(p[1], `👮‍♂️ <b>Ответ админа:</b>\n\n${p.slice(2).join(' ')}`, { parse_mode: 'HTML' }).catch(()=>{});
});



// Обработчики кнопок кабинета
bot.action('start_registration', (ctx) => { ctx.deleteMessage(); ctx.scene.enter('REGISTER_SCENE'); });

// --- ВОЗВРАЩАЕМ ПРОПАВШИЕ КОМАНДЫ (30+ СТРОК) ---



// --- АВТОМАТИЧЕСКАЯ ЗАПИСЬ ПОСЛЕ ОПЛАТЫ (WEBHOOK) ---
async function handleSuccessfulPayment(session: any) {
  const { telegramId, eventId, voucherId } = session.metadata;
  const user = await db.query.users.findFirst({ where: eq(schema.users.telegramId, parseInt(telegramId)) });
  const event = await db.query.events.findFirst({ where: eq(schema.events.id, parseInt(eventId)) });
  
  if (!user || !event) return;

  // Если запись уже создана кнопкой "Я оплатил", выходим и ничего не прибавляем!
  const existing = await db.query.bookings.findFirst({ 
    where: and(eq(schema.bookings.userId, user.id), eq(schema.bookings.eventId, event.id)) 
  });
  if (existing) return; 

  await db.insert(schema.bookings).values({ userId: user.id, eventId: event.id, paid: true });
  await db.update(schema.events).set({ currentPlayers: (event.currentPlayers || 0) + 1 }).where(eq(schema.events.id, event.id));

  // 4. Гасим ваучер, если он был
  if (voucherId) await db.update(schema.vouchers).set({ status: 'used' }).where(eq(schema.vouchers.id, parseInt(voucherId)));

  // 5. Бонус рефералу (если есть)
  if (user.invitedBy) {
    const inviter = await db.query.users.findFirst({ where: eq(schema.users.id, user.invitedBy) });
    if (inviter) {
      await db.insert(schema.vouchers).values({ userId: inviter.id, status: 'approved_10' });
      bot.telegram.sendMessage(inviter.telegramId, `🎉 Твой друг оплатил игру! Тебе начислена скидка -10 PLN!`).catch(() => {});
      await db.update(schema.users).set({ invitedBy: null }).where(eq(schema.users.id, user.id));
    }
  }



// 6. Пишем юзеру радостную весть
 // 6. Пишем юзеру радостную весть
  // 6. Пишем юзеру радостную весть
  // 6. Пишем юзеру радостную весть
  let messageText = `🎉 <b>Оплата подтверждена! Ты в игре!</b>\n\n` +
                    `📍 <b>Важные правила (коротко):</b>\n` +
                    `• <b>Отмена и перенос:</b> Возможны только за <b>36 часов</b> до начала. Позже сумма «сгорает».\n` +
                    `• <b>Атмосфера:</b> Мы за уважение и классный вайб. 🥂\n` +
                    `• <b>Локация:</b> Адрес и инструкция придут за <b>3 часа</b> до начала игры.\n\n`;

// Просим историю ТОЛЬКО для формата Talk & Toast
  if (event.type.includes('talk_toast')) {
      messageText += `🤫 <b>Кстати!</b> Для нашей секретной викторины нам нужна твоя история. Пожалуйста, зайди в 👤 <b>Личный кабинет</b> -> <b>Добавить историю</b> и напиши один забавный или странный факт о себе. Это сделает вечер в разы круче! ✨\n\n`;
  }

  messageText += `Напоминаем, что еда и напитки оплачиваются отдельно.\n\nДо встречи! 😎`;

// Отправляем сообщение
  await bot.telegram.sendMessage(user.telegramId, messageText, { parse_mode: 'HTML' }).catch(() => {});
// СНАЧАЛА проверяем тип игры, а ПОТОМ отправляем
  // 7. Удаляем из брошенной корзины
  PENDING_PAYMENTS.delete(`${user.id}`);
}

// --- ЗАЩИТНЫЙ ЩИТ ОТ ОШИБОК (чтобы бот не падал) ---
bot.catch((err: any, ctx) => {
  const errorDescription = err.description || err.message || "";
  if (errorDescription.includes("message is not modified")) {
    return; // Просто игнорируем, если текст кнопки тот же
  }
  console.error(`Ошибка в боте (${ctx.updateType}):`, err);
});

// --- ЗАПУСК СЕРВЕРА ---
const app = express();

// СТРОГО ПЕРЕД express.json()
app.post('/stripe-webhook', express.raw({ type: 'application/json' }), async (req, res) => {
  const sig = req.headers['stripe-signature'];
  let stripeEvent;
  try {
    stripeEvent = stripe.webhooks.constructEvent(req.body, sig!, process.env.STRIPE_WEBHOOK_SECRET!);
  } catch (err: any) { 
    return res.status(400).send(`Webhook Error: ${err.message}`); 
  }

  if (stripeEvent.type === 'checkout.session.completed') {
    await handleSuccessfulPayment(stripeEvent.data.object);
  }
  res.json({ received: true });
});;

app.use(express.json());
app.use(bot.webhookCallback('/telegraf-webhook'));

const PORT = process.env.PORT || 3000;
const WEBHOOK_URL = process.env.TELEGRAM_WEBHOOK_URL;

app.listen(PORT, async () => {
    console.log(`🚀 Сервер Алгоритма запущен на порту ${PORT}`);
    if (WEBHOOK_URL) {
        await bot.telegram.setWebhook(`${WEBHOOK_URL}/telegraf-webhook`);
        console.log(`📡 Вебхук установлен на: ${WEBHOOK_URL}`);
    }
});

process.once('SIGINT', () => bot.stop('SIGINT'));
process.once('SIGTERM', () => bot.stop('SIGTERM'));
